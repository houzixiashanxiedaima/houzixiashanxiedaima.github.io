---
title: GitHub Releases 日报 - 2026-08-31
description: 2026-08-31 GitHub Releases 日报：openclaw/openclaw、nicobailon/pi-subagents、jdx/mise 等16个项目更新 | 新增历史对话搜索功能，支持按精确词组查找并重新打开相关消息 | 子代理启动与状态更新减少重复文本，降低长任务上下文占用
author: Release Bot
pubDatetime: 2026-08-31T08:00:00.000Z
draft: false
tags: ["releases", "daily", "automation", "openclaw", "pi-subagents", "mise", "oh-my-posh", "open-design"]
category: 开源追踪
ogImage: /images/covers/cover-2026-08-31.webp
---
## 2026-08-31 更新摘要

**统计信息**
- 由 Qwen/Qwen3.8-27B (16) 总结
- 监控仓库: 19 个
- 今日更新: 13 个项目 (16 个版本)
- 生成时间: 2026-09-01 03:38:09 (北京时间)


### [openclaw/openclaw v2026.8.1](https://github.com/openclaw/openclaw/releases/tag/v2026.8.1) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增历史对话搜索功能，支持按精确词组查找并重新打开相关消息
- 支持在配对设备或云端工作器上运行任务，可迁移会话工作区并复用预热机器
- 新增持久化会话进度卡片，支持跨重载跟踪子代理活动及累积编辑
- 优化结构化问题响应体验，支持网页/原生卡片、消息按钮或纯文本回复及跳过路径
- 新增交互式结果与仪表盘，支持聊天小部件固定、权限授予及导出为图片
- 新增私有凭据请求功能，通过掩码提示获取凭据，避免值进入聊天或模型上下文
- 支持一次性批准周期性任务，允许检查或撤销自动化权限，变更时需重新批准
- 增强音视频支持，媒体在上传、生成、播放及重载间保持关联，支持原生控制
- 新增 OpenAI 长上下文显式配置选项，区分运行时预算与模型原生容量
- 启用个人对话回忆，在 Active Memory 下默认检索同代理私有对话上下文
- 启用基于模型的后台记忆整合（Grounded Dreaming），将合格材料提升至长期记忆
- 启用自动自学习，默认捕获强复用经验并应用扫描器批准的新技能
- 调整会话重置默认行为，未配置策略时跨越空闲期及日期边界保留对话
- 新增 CPU 缩放前台并发，根据可用 CPU 并行度设置默认顶层代理并发数
- 新增显式模型白名单，分离别名与每模型设置，支持每代理策略及提供商通配符

**Breaking Changes**
- 移除捆绑的 OpenProse 插件和 /prose 命令，需运行 openclaw doctor --fix 清理配置
- 迁移 OpenAI 路由，需运行 openclaw doctor --fix 将 codex/* 等模型引用迁移至 openai/*
- 即将弃用外部插件 SDK 子路径，需在 2026-09-01 前迁移至新的 API 端点

**问题修复**
- 修复配对设备不可用时的状态显示，保留启动超时或显式取消结果
- 修复离线桌面工作器在 Computer Use 准备阶段断开时的状态报告
- 修复数据库隔离恢复机制，防止损坏的数据库清除隔离记录
- 修复提供商迁移过程中的模型选择丢失问题，报告恢复步骤而非静默删除
- 修复 Provider SecretRefs 处理，保持凭据引用与解析字节分离，避免阻塞其他提供商
- 修复认证配置文件模型选择，保持凭据配置文件与模型 ID 分离，拒绝错误提供商的配置
- 修复 SQLite 数据安全问题，防止 WAL 分裂脑清理导致数据库损坏
- 修复新版数据库状态处理，停止重启循环，拒绝竞争共享状态模式变更
- 修复工作区安全性，保留重叠的 OpenShell 上传并保持可见生成与其父仓库关联
- 修复私有提示保护，防止复制的私有提示上下文出现在最终或流式回复中
- 修复受保护凭据出口，在运行结束时关闭代理连接、上游请求及绕过隧道
- 修复凭据请求卡片，保留精确秘密值，显示警告，允许主机列表编辑及取消请求
- 修复人工输入等待，保持运行在已接受问题或凭据输入期间不进入陈旧会话恢复
- 修复文件编辑安全性，拒绝编辑非 UTF-8 文件以防止无效字节被静默重写
- 修复文件写入比较逻辑，确保无效 UTF-8 不会导致请求的覆盖被静默跳过


### [nicobailon/pi-subagents v0.61.0](https://github.com/nicobailon/pi-subagents/releases/tag/v0.61.0) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 子代理启动与状态更新减少重复文本，降低长任务上下文占用
- 工作流作者可使用命名资源以明确权限边界，并支持顶层容量覆盖配置
- 新增扩展拥有的命名工作流资源，便于区分可信工作流资源与原始脚本
- 新增仅限工作流的 globalConcurrencyLimit 和 maxSubagentSpawnsPerRun 覆盖选项
- 新增 bg_wait 作为主要后台等待工具，subagent_wait 保留为兼容别名
- 模型列表现在显示已发现和运行时注册代理的有效模型映射
- 精简默认子代理提示指南至五条面向父级的条目，高级细节保留在打包指南中
- 减少委托运行期间的重复心跳状态更新，保留前台进度和完整终端响应
- 澄清 fallbackModels 处理提供商/模型超时但不处理运行级 timeoutMs/maxRuntimeMs 过期
- 向后台工作提供商传递请求的会话和时间戳上下文，避免列出无关会话
- 避免 Fleet 刷新期间重复的外部运行显示规范化，同时验证外部替换或变更的记录
- 澄清 oracle 和顶级推理模型是升级工具而非常规默认值
- 安全时从恢复的内存状态提供广泛的 RPC 状态请求，保留目标状态和转录行为

**Breaking Changes**
- subagent_wait 标记为弃用，建议迁移至 bg_wait

**问题修复**
- 隔离损坏的活动异步状态文件，确保有效运行可用且损坏的运行工件得以保留
- 减少运行中工作流的异步小部件更新抖动，通过重绘动画帧而不重新安装小部件来合并关闭状态刷新
- 避免小部件渲染期间重复的分阶段工作流投影
- 保留持久化的仅文件子报告，并在格式错误的接受元数据后继续只读工作流审查
- 在回退和分叉准备过程中保留模型来源，使可用的回退对不可用的配置主模型生效，无效显式模型仍失败关闭
- 停止隐藏的 125ms 旋转器重绘，保留进度刷新和一秒钟动画帧


### [jdx/mise v2026.8.15 - v2026.8.16](https://github.com/jdx/mise/releases) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增 shims 配置，支持通过 wrappers 拦截普通命令并替换为不同命令、参数和环境变量
- acli、mimirtool 和 specstory 不再限制于 Linux 和 macOS，现可在 Windows 上安装和运行
- llama.cpp 解析现在包含预发布版本，以适配上游将滚动构建标记为 GitHub 预发布的变更
- 新增 dotfiles 预览与捕获工作流，支持 diff、批量 add --changed、git 跟踪清单及 profile 切换时的自动协调
- mise config set 支持幂等列表更新（--append/--remove）及显式 --global/--system 目标定位
- mise use 新增按工具配置的 --postinstall 钩子
- 升级安全增强：被替换的工具版本保留宽限期而非立即删除，便于从错误升级中恢复
- task_source_files(only_changed=true) 仅返回自上次成功以来写入的源文件，便于针对变更运行 linter/formatter
- mise outdated --json 新增 release_url 字段指向发布页面
- Homebrew bootstrap 瓶子现在并发下载（受 MISE_JOBS 限制），提升安装性能
- 注册表新增工具：pkgx, onefetch, syncthing, ols, spin-framework, cursor-agent, 7zip…
- magika 和 sbt 现从 GitHub releases 安装，pre-commit 在 Windows 上使用 pipx 后端
- config 模板可引用 config_source 获取配置文件绝对路径

**问题修复**
- 修复 Elvish 中 mise env 和 mise activate --shims 输出无效引号的问题，解决 Windows 上的路径分隔符和转义字符错误
- 修复 fish 中 mise env -s fish 在 Windows 上因分号分隔符导致的路径截断问题，并移除空 PATH 段
- 改进 HTTP 错误信息，当平台无 URL 时列出工具实际声明的所有平台键，包括拼写错误
- 优化升级提示，当存在超出配置范围的新版本时，列出受影响工具及其当前和可用版本
- 修复 task 双破折号参数被外层 CLI 解析器消费丢失的问题，支持 double_dash="preserve" 保留参数
- 隔离嵌入式 Aube npm 安装为独立单包工作区，防止 pnpm-workspace.yaml 重定向或修改外部工作区
- bootstrap 仓库状态检查现在遵守全局 --jobs 限制，以有界并发运行只读 git 检查
- 修复 UTF-16 编码 .env 文件和任务文件被静默丢弃或提示不清的问题
- 修复 mise install --dry-run 错误声称可安装当前平台不支持的工具
- 修复 mise exec 未清晰报告当前平台不可用工具的问题
- 配置合并时保留请求特定的工具选项，裸 tool_alias 值通过注册表解析，多配置的最小发布年龄排除项正确合并
- 修复 legacy lockfile 版本平局处理及其他后端链接忽略问题
- monorepo 中惯用版本文件从任务自身配置根目录解析
- 修复多个任务排序问题，包括注入/依赖任务槽位、自链接跳过及 Windows taskkill 输出抑制
- shims/generate 完整传递参数，解释无 Windows launcher 的 --mise-bin 路径，跳过非可执行系统回退
- Windows 上无法使用 junction 命名文件时复制原始二进制，路径过长下载失败提供明确说明
- 目标不再存在的链接现在可以显示和移除
- ignore 列表按其条目指向的内容读取，匹配 trust 链接存储方式
- watched 进程收到信号时恢复终端
- pwsh 反激活前导不再污染 $Error
- 检测 versions host 后的滚动更新，重试过期的缓存证明，ls-remote 记录错误导致失败而非返回陈旧结果
- 修复 npm aube 支持的悬空工具安装，bootstrap prune 期间保留 Brew cask 依赖
- azure CLI 包含 pip 以支持扩展，Windows/arm64 可回退到 x64 后端
- doctor 自我更新残留检查门控，避免在其他地方管理更新的安装中触发
- 修正 JSON schema 使 registry、plugin、tool-option 和 settings 定义与实际构建消费者接受的一致


### [JanDeDobbeleer/oh-my-posh v31.1.0 - v31.1.1](https://github.com/JanDeDobbeleer/oh-my-posh/releases) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 扩展原生 gitstatus 引擎功能

**问题修复**
- 修复 pwsh 中异步初始化竞态问题，通过 _ompInitialized 防止模块重入


### [nexu-io/open-design open-design-v0.21.1](https://github.com/nexu-io/open-design/releases/tag/open-design-v0.21.1) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 取消强制登录，支持直接从首页选择本地 CLI 或 BYOK 开始创作
- 新增 Open Design Labs，可在设置中启用 Harness 以尝试新一代生成策略
- 优化图像生成，当原型涉及真实人物、产品或地点时自动搜索并使用真实参考图
- 增强移动端界面理解能力，为日历、统计、导航等组件提供专用结构
- 团队工作区新增后台追赶预算上限选项，防止后台任务无限增长
- 优化项目列表与深链接读取性能，引入短期缓存并即时失效机制
- 优化首页启动性能，合并重复目录读取并延迟加载自动化区域数据
- 云账户注销后保留本地 Agent 和 BYOK 配置，确保离线可用性
- 改进错误提示，针对不兼容握手、并发限制及模型拒绝提供更准确的修复建议
- 优化上游配额与速率限制信号处理，避免被通用无结果分类掩盖
- 修复澄清表单在渲染后导致策略任务终止的问题，确保可正常回答
- 优化 HTML 预览与导出，正确识别文档边界，避免破坏嵌套脚本或属性中的标记
- 修复 PPTX 导出背景透明问题，增加空捕获重试与明确失败提示
- 优化聊天窗口调整大小手柄交互，避免干扰滚动边缘操作
- 团队余额显示跟随当前查看的工作区，不再回退至个人钱包
- 私有项目的 Vela 图像和视频生成保持所属工作区的计费范围不变
- 创建设计系统后返回按钮恢复原始上下文，而非跳转至通用页面
- 插件描述与恢复文案遵循应用语言设置，并提供可预测的英文回退
- Go Plan 停用通知根据用户状态精准推送，且文案布局遵循所选语言
- 开发模式不再占用系统级 opendesign:// 链接处理器，打包版保持正常授权流程
- 未登录桌面用户在右上角集群中可见待安装更新，位置与登录用户一致

**Breaking Changes**
- 仓库拥有的 Nix flake 已在 main 分支退役，如需使用请固定至 open-design-v0.21.1 标签

**问题修复**
- 修复停止运行时的进程树清理问题，防止子进程继续消耗资源
- 修复缺失本地引擎时返回应用外壳而非清晰连接错误的问题
- 修复 Go Plan 停用通知硬编码中文的问题，现遵循应用语言设置


### [EKKOLearnAI/hermes-studio v0.7.13 - v0.7.14](https://github.com/EKKOLearnAI/hermes-studio/releases) <sup>Qwen/Qwen3.8-27B</sup>

**安全修复**
- 加固 Runtime 回退验证机制

**问题修复**
- 限制文件读取范围并使用原生命令
- 修复 Windows Runtime 文件系统操作锁定时的重试机制
- 修复创建 Hermes 聊天时复用 Agent 状态的问题
- 统一 Hermes CLI 与桥接运行时选择逻辑


### [steipete/CodexBar v0.56.2](https://github.com/steipete/CodexBar/releases/tag/v0.56.2) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 优化本地成本计算性能，减少解析原生会话时间戳和验证 Codex 历史追加时的后台 CPU 占用
- 优化 Codex 扫描性能，减少重复解码缓存的成本历史，同时保留存储的总计并检查数据库和文件系统变更
- 优化 Claude 和 Vertex AI 性能，减少读取本地转录元数据时的 CPU 占用，保留提供商检测、Unicode 处理和 token/成本总计

**问题修复**
- 修复本地成本在合并报告或重新打开缓存历史时丢失 token 细分、推理、请求计数和定价覆盖的问题
- 修复 Codex 在移除 fork 文件导致现有缓存中遗留父级发现时，成本历史追赶恢复失败的问题
- 修复 Codex 在仅刷新额度时未保留待处理的每周重置证据，导致低使用量确认在重启后丢失的问题
- 修复图表在 token/成本、信用使用、信用历史和计划历史子菜单中端点日期显示不可读的问题
- 修复 Agent 会话在扫描预算过期时未停止 Codex 元数据丰富和 Pi/OMP 路径解析…
- 修复 OpenCode Go 对本地估算配额显示误导性的节奏和耗尽建议，同时保留百分比、重置和成本历史
- 修复 OpenRouter 从使用仪表板而非信用设置打开 Activity 的问题


### [OthmanAdi/planning-with-files v3.12.1](https://github.com/OthmanAdi/planning-with-files/releases/tag/v3.12.1) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 运行 .planning/ 内的认证辅助程序时，现在会更新 slug .attestation 文件，而非创建旧版文件
- macOS 系统别名、Windows 活动计划指针、Unix PowerShell 拒绝机制及可信解释器回退在支持矩阵中行为保持一致


### [xishang0128/sparkle 1.26.8](https://github.com/xishang0128/sparkle/releases/tag/1.26.8) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 支持系统通知错误详情
- 支持服务运行模式和错误详情
- 对路由预加载进行节流以优化性能
- 优化主进程和渲染进程启动速度
- 限制外部导航方案以提升安全性
- 在内部管理窗口状态
- 现代化 Monaco 编辑器集成
- 支持窗口拖拽模式
- 升级 sparkle native 到 0.1.2
- 更改 SettingItem 中更新按钮的标题
- 显示带流量的托盘图标
- 支持用于更新的 GitHub 令牌
- 更新 AUR 软件包及自动化流程
- 更新依赖项

**问题修复**
- 修复 pacman 启动器所有权问题
- 修复 Windows ARM64 NSIS 安装程序问题（#250）
- 杂项：清理 install-electron 脚本
- CI：在 loong64 上更新 electron 和 electron-builder


### [shd101wyy/vscode-markdown-preview-enhanced 0.8.32](https://github.com/shd101wyy/vscode-markdown-preview-enhanced/releases/tag/0.8.32) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增设置 markdown-preview-enhanced.enablePreviewContextMenu…
- 新增 AI 翻译预览功能，支持将当前 Markdown 预览翻译为其他语言并切换回原文，且按块缓存翻译结果
- 预览 Webview UI 本地化，界面组件（如上下文菜单、页脚等）现跟随 VS Code 显示语言，支持十种语言
- 新增显式启用自定义预览脚本的设置 markdown-preview-enhanced.enablePreviewScripts，恢复对特定脚本注入的支持
- 浏览器/HTML 导出现在遵循阅读者的系统颜色方案，仅在纸质输出时强制使用浅色主题
- 升级 mermaid 至 11.17.2，包括离线预览捆绑包和 CDN 回退机制

**安全修复**
- 为预览 Webview 添加 Content-Security-Policy (CSP)，限制脚本和样式加载源，阻止对象源和表单操作
- 加固预览 Webview 中 @import "*.js" 的脚本生成逻辑，防止远程 URL 被意外执行

**问题修复**
- 修复在标签页间切换时仅预览模式滚动位置丢失的问题，保留 Webview 上下文以防止重载
- 修复在无 xdg-utils 的 WSL 发行版上“在浏览器中打开”无响应的问题
- 修复引用块或列表标记泄漏到 $…$ 显示数学公式中的渲染错误
- 修复项目路径中包含 # 字符导致预览中图片加载失败的问题
- 修复 front matter 中 toc: ordered: true 选项无效的问题，目录现在可以正确渲染为有序列表
- 修复嵌入 d2 图表在引用相对图片时渲染失败的问题，现在会回退为纯文本代码块
- 优化长行文档的渲染性能，将二次方复杂度降低为线性时间


### [xchacha20-poly1305/husi v2.0.7](https://github.com/xchacha20-poly1305/husi/releases/tag/v2.0.7) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 仪表盘新增编辑状态卡片
- JSON 编辑器在格式化后保留注释
- 翻译更新（感谢 @GflowerG）

**问题修复**
- 修复与改进


### [rikkahub/rikkahub 2.4.16](https://github.com/rikkahub/rikkahub/releases/tag/2.4.16) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增自动重试开关，可控制网络错误时是否自动重试
- 提升语音播放稳定性，并将默认播放速度移至常规设置
- 简化快速模型设置，支持配置思考级别，并统一用于标题和聊天建议
- 改进聊天附件、头像、背景及图片生成中的图片选择体验

**Breaking Changes**
- 网页和矢量图代码默认不再自动预览，减少意外渲染
- 移除不再可用的硅基流动余额查询

**问题修复**
- 修复 Google 模型同时使用多种工具时的异常，并改善部分服务的授权登录
- 修正聊天气泡透明度的百分比显示


### [SagerNet/sing-box v1.14.0](https://github.com/SagerNet/sing-box/releases/tag/v1.14.0) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- iOS 和 tvOS 客户端重新上架 App Store，应用名称变更为 sing-box MT
- 新增 OpenVPN 客户端和服务端支持，兼容标准 OpenVPN 协议及多种加密模式
- 新增 OpenConnect 客户端支持 Cisco AnyConnect、GlobalProtect 等主流 VPN 服务器
- 新增 Snell 入站和出站支持，保留其多路复用与流量特征优势
- L3 入站（TUN/WireGuard/Tailscale）的 TCP/UDP 流量可直接转发至 L3 端点，无需 L3-L4 转换
- 新增 bridge 出站，直接通过网卡转发 TUN 等 L3 端点的 TCP/UDP/ICMP 流量
- 新增 network_namespaces 选项，支持无 root 权限创建 Linux 网络命名空间
- 新增 gRPC API 服务，提供状态监控、日志查看、连接追踪及 Dashboard 下载等功能
- 新增 sing-box api CLI 命令，作为 API 服务的命令行客户端
- 新增 USB/IP 服务端和客户端，支持跨平台导出和导入 USB 设备
- 新增 Hysteria Realm 服务，用于 Hysteria2 NAT 穿透的地址发现与 UDP 打洞
- Hysteria2 客户端默认模拟 Chrome QUIC 握手，降低被指纹识别的风险
- DNS 规则新增响应匹配字段，支持对已评估 DNS 响应的 rcode、answer 等进行匹配
- DNS 规则中的 ip_version 和 query_type 现在在所有评估路径中生效，包括内部域名解析
- 优化乐观 DNS 缓存，立即返回过期缓存并在后台刷新，降低重复查询延迟
- 新增 mDNS DNS 服务器，支持本地网络组播查询
- 新增基于 MAC 地址和主机名的局域网设备匹配规则项
- TUN 入站新增 dns_mode 和 dns_address，默认劫持模式现设置系统原生接口 DNS
- 新增 UDP NAT 配置字段，控制 TUN/TProxy/WireGuard 的 NAT 映射与过滤行为
- UDP 连接首包在 pre-match 阶段可用，支持直接进行协议嗅探
- 新增顶层 http_clients 选项，定义可复用的 HTTP 客户端配置
- HTTP/2 和 QUIC 参数在 QUIC 出站和 HTTP 客户端间共享
- 新增 outbound TLS spoof 字段及相关路由规则动作
- Windows 构建新增 windows TLS 引擎值，通过 Schannel 处理 TLS 握手
- Tailscale 端点新增 ssh_server 字段，支持在 tailnet 端口 22 运行 SSH 服务器
- sing-box 配置现提供 JSON Schema，支持编辑器自动补全和验证
- rule-set tag 字段现接受标签列表，支持一次性定义多个共享选项的规则集
- 新增 sing-box for Desktop 桌面客户端，支持 Windows 10+ 和 Linux 多架构
- iOS 越狱版客户端现以 .ipa 形式分发
- Linux 软件包和 Docker 镜像现分为 stable、beta、testing、oldstable 四个发布轨道

**Breaking Changes**
- iOS/tvOS 用户需从旧版 sing-box VT 迁移安装新版 sing-box MT
- 编译 sing-box 1.14.0 现要求至少 Go 1.25
- Hysteria v1 调优字段 recv_window_conn 等已弃用，将在 1.16.0 移除
- TLS 内联 ACME 选项已弃用，需改用 certificate_provider 证书提供者系统



**帮助我们改进**

对今日摘要质量有建议? 欢迎反馈！

- [反馈摘要质量](https://github.com/houzixiashanxiedaima/AstroBlog/issues/new)
- [评价今日日报](https://github.com/houzixiashanxiedaima/AstroBlog/issues/new)

您的反馈将帮助我们优化 AI Prompt,提升摘要准确性
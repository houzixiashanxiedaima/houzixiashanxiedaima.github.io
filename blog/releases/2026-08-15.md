---
title: GitHub Releases 日报 - 2026-08-15
description: 2026-08-15 GitHub Releases 日报：nextai-translator/nextai-translator、PowerShell/PowerShell、srwi/EverythingToolbar 等20个项目更新 | 新增 UI 字体大小设置，可按需求缩放界面 | 归档版本升级至 1.2.6
author: Release Bot
pubDatetime: 2026-08-15T08:00:00.000Z
draft: false
tags: ["releases", "daily", "automation", "nextai-translator", "PowerShell", "EverythingToolbar", "open-design", "MiaoYan"]
category: 开源追踪
ogImage: /images/covers/cover-2026-08-15.webp
---
## 2026-08-15 更新摘要

**统计信息**
- 由 gpt-oss-120b (19), deepseek-ai/DeepSeek-V4-Flash (1) 总结
- 监控仓库: 21 个
- 今日更新: 14 个项目 (20 个版本)
- 生成时间: 2026-08-16 04:38:50 (北京时间)


### [nextai-translator/nextai-translator v0.6.37 - v0.6.38](https://github.com/nextai-translator/nextai-translator/releases) <sup>gpt-oss-120b</sup>

**新内容**
- 在模型列表中支持推理模型，已完成韩语翻译
- OpenAI 模型列表新增 o 系列推理模型 o1、o3、o4-mini
- 完成词典功能缺失的韩语翻译
- 新增 UI 字体大小设置，可按需求缩放界面
- 当 Windows 缺少或损坏 WebView2 Runtime 时，显示明确错误对话框并提供打开官方修复下载页的按钮


### [PowerShell/PowerShell v7.4.19 - v7.5.10](https://github.com/PowerShell/PowerShell/releases) <sup>gpt-oss-120b</sup>

**新内容**
- 升级 .NET SDK 至 9.0.317，更新发布分支并回溠 Windows 构建流水线，归档版本号至 1.2.6
- 归档版本升级至 1.2.6
- 仅为 MSIX 包提供本地化资源
- 为 arm64 架构生成最小体积包
- .NET SDK 10.0.302 用于构建
- 升级 .NET SDK 至 8.0.424 并改进构建/打包流程

**问题修复**
- 修复 pwsh -file 对高级函数脚本的点源行为
- 若干构建相关修复（OneBranch 输出变量、EXE 签名检查、冲突解析错误）
- 若干修复（dot‑sourcing 行为、TimeZone 测试）


### [srwi/EverythingToolbar 3.2.0](https://github.com/srwi/EverythingToolbar/releases/tag/3.2.0) <sup>gpt-oss-120b</sup>

**新内容**
- 新增搜索选项：匹配变音符、前后缀匹配、忽略标点和空格
- 过滤器现在会根据 Filters.csv 中的配置触发排序方式和匹配选项
- 开始菜单搜索拦截仅捕获可打印字符，快捷键等键保持原有行为
- 任务栏窗口对齐选项被禁用时显示提示
- 改进设置子标题的可读性
- 通过 Crowdin 改进翻译质量
- 提供构建可追溯性证明，支持验证安装包未被篡改

**问题修复**
- 恢复全局快捷键处理，修复快捷键未能将搜索窗口置前的问题


### [nexu-io/open-design open-design-v0.19.2](https://github.com/nexu-io/open-design/releases/tag/open-design-v0.19.2) <sup>gpt-oss-120b</sup>

**新内容**
- 一键安装 DeepSeek Harness，支持 macOS、Linux、Windows PowerShell 与 CMD。
- 安装脚本采用 shell 安全路径，Windows 应用及 DSH_HOME 包含空格亦可正常连接。
- 离线安全的 HTML 导出，打包本地图片、样式、字体、模块等为单文件。
- 长时间代理运行保持响应，聊天流式输出同时后台批处理持久化与 React 更新。
- 支持仅使用项目 ID 进行 od export，无需工作区或成员标志。
- 结构化 Design System 3.0 运行时，提供意图映射、组件实现、令牌与规则。
- 代理可在生成前解析组件，仅加载声明文件并验证复用、必需状态、令牌使用等。
- 新增 13 个 DeepSeek Harness 设计插件，按视觉、生成 UI、工作流、预览等分类并提供本地化页面。
- 包装应用搜索标准 nix-darwin、NixOS、系统及用户配置目录以发现本地安装的 CLI。
- 首页创建行优先展示常用项目类型，提升创建体验。
- 生产部署后清除活动主机并设置短 TTL，降低访问陈旧页面的时间。
- 取消 Harness 运行后立即标记为已取消，保留提供者错误详情并记录正确的提供者与模型。
- 预览与工作区 UI 改进：演示恢复、反向代理预览、评论桥等待提交的预览 URL、移动端预览无闪烁、全屏显示等。
- Sidecar 支持内部主机白名单，确保在受限内部主机下的部署可用。
- 统一 DeepSeek Pro 与 Flash 活动文案，保持产品与本地化登陆页一致。

**问题修复**
- 解决缺失或过大依赖导致的导出错误，改为返回明确错误信息。
- 取消运行状态不再显示为 Working、Done 或 Run failed，避免误导。
- 缺少 DeepSeek 凭证时提供设置步骤，避免出现 [object Object]。
- 长结构化事件流在输出后不再让聊天保持忙碌，提升交互流畅度。
- 项目导出避免重复的工作区上下文失败，提高稳定性。
- Chromium 中断 about:srcdoc 时演示可恢复，防止预览崩溃。
- 通过反向代理的预览正常工作，评论桥在准备就绪前不提前报告。
- 移动端评论预览关闭时不再闪烁，幻灯片预览填满视图且不出现信箱框。
- 消息中心行重新显示已配置的 HTTP(S) 动作。
- 不安全的动作 URL 被隐藏，提升安全性。


### [tw93/MiaoYan V4.2.0](https://github.com/tw93/MiaoYan/releases/tag/V4.2.0) <sup>gpt-oss-120b</sup>

**新内容**
- 新增官方 MiaoYan Agent Skill，支持按语法、附件、PPT 与 CLI 规范处理笔记
- 改进 PDF 导出，Mermaid 图表不再重复渲染，标题沿用当前笔记字体
- 改进 HDR 照片在 Markdown 预览中的显示效果，画面不再异常过曝

**问题修复**
- 修复已删除笔记被延迟保存重新创建的问题，切换侧栏或重启后不再出现
- 修复复杂 LaTeX 公式渲染，支持下标、绝对值和自适应括号在预览与 PPT 中正确组合
- 修复删除笔记后侧边栏文件夹横向偏移，列表始终贴齐窗口宽度
- 修复 PPT 本地图片丢失，及窗口置顶时偏好设置被遮挡的问题


### [rorkai/App-Store-Connect-CLI 4.3.0 - 4.4.2](https://github.com/rorkai/App-Store-Connect-CLI/releases) <sup>gpt-oss-120b</sup>

**新内容**
- 支持受限工作目录
- 新增 Platform API v1 基础功能
- 新增 Platform campaign 资源
- 新增 Platform v1 地图和资产
- 新增 Platform v1 报告与优化
- 新增 apps wall P2P 视图
- 支持 Xcode 发布测试导出
- 管理 Developer Portal 应用组
- 在隔离签名环境中运行命令
- 同步私有身份
- 对 ad hoc 配置文件进行对账
- 准备已验证的 ad hoc 包
- 发布已验证的安装链接
- 编排已验证的 ad hoc 安装
- 新增应用到墙：Sleep Analysis - Lunomia、hora Calendar、Trailbot: MTB Trail Conditions
- 升级 Go 语言至 1.26.6
- 升级依赖（golang.org/x/text 0.41.0、github.com/bitrise-io/go-pkcs12 0.1.0…

**Breaking Changes**
- 废弃 Campaign Management API v5，需迁移至新版本

**问题修复**
- 在 Xcode 导出预检时隔离主机 Xcode 环境
- 在审查提交项中请求 appStoreVersion 关联
- 修复广告查询：验证关键字选择器并在 v1 查询中拒绝旧字段
- 分析模块排除重新下载和更新计入下载量
- 构建模块拒绝意外的 add-groups 参数
- 加固 Web 应用组变更安全性
- 支持分离的制品根目录


### [ccusage/ccusage v20.0.20](https://github.com/ccusage/ccusage/releases/tag/v20.0.20) <sup>gpt-oss-120b</sup>

**新内容**
- 新增 Grok Build CLI 使用适配器
- pricing：提交 codex 回退快照的真实路径
- pricing：停止对上游未发布模型的断言
- pricing：从作者目录获取 models.dev 费率


### [xchacha20-poly1305/husi v1.4.5](https://github.com/xchacha20-poly1305/husi/releases/tag/v1.4.5) <sup>gpt-oss-120b</sup>

**新内容**
- 新增可在 sing-box 中禁用 Chrome Parrot 功能
- 修复若干问题并进行改进


### [sxyazi/yazi v26.8.15](https://github.com/sxyazi/yazi/releases/tag/v26.8.15) <sup>deepseek-ai/DeepSeek-V4-Flash</sup>

**新内容**
- 常规改进和修复


### [steipete/CodexBar v0.50.0](https://github.com/steipete/CodexBar/releases/tag/v0.50.0) <sup>gpt-oss-120b</sup>

**新内容**
- 在 macOS 上优先使用 Cursor.app 的只读本地会话，自动模式下安全持久化并在账户不匹配时提示。
- 在每日状态菜单图表中加入代币/费用切换开关，默认显示 Codex 本地代币总数，未完成历史标记为刷新中。
- 在菜单栏布局中添加紧凑的预测剩余代币显示，仅展示预计时长。

**问题修复**
- 修复 Codex 在使用 Amazon Bedrock 等自定义后端时的登录说明，改为提供准确的速率限制指引。
- 修复 macOS 桌面样式中 Burn Down 小部件的渐变背景容器被移除的问题。
- 修复 Grok 将当前每周信用窗口标记在重置附近，而不再使用模糊的 “Credits” 标题。
- 修复 Codex 在没有会话或每周速率限制窗口时，提供商切换器中未显示企业账户月度剩余信用，并保持配额指示可见。
- 修复 Gemini 在本地 OAuth 恢复无法使用 Gemini CLI 时，提供 Antigravity 迁移选项的功能。
- 修复提供商状态在获取失败时仍在菜单中显示传输错误，改为保留上一次成功的状态直至新获取成功。


### [nicobailon/pi-subagents v0.50.0](https://github.com/nicobailon/pi-subagents/releases/tag/v0.50.0) <sup>gpt-oss-120b</sup>

**新内容**
- 新增多项功能，包括 Orca 进度标签页、FleetView 外部作业显示、RPC 状态快照、前台分离快捷键等。
- 改进若干行为和指南，如保留子代理可恢复性、完成写入器使用 resume、oracle/advisor 提示处理、子代理列表状态显示、异步结果索引优化等。

**问题修复**
- 修复 Orca 进度标签页回压导致的镜像截断、错误 .log artifact 广告、长路径导致 FleetView 检查失败等问题。
- 修复输出路径覆盖、前台分离提示、模型注册检查、Herdr 命令安全执行、agentContract.version 枚举、工作流子代理状态显示…


### [nicobailon/pi-web-access v0.23.0](https://github.com/nicobailon/pi-web-access/releases/tag/v0.23.0) <sup>gpt-oss-120b</sup>

**新内容**
- 新增可选的 authFetch 配置，用于本地浏览器 Cookie 的 fetch_content 请求
- 新增 Firecrawl 作为 web_search 提供者，使用现有 Firecrawl 配置
- 新增 macOS Brave 浏览器 Cookie 支持，用于 Gemini Web 的可选访问
- 本地视频检测改为使用显式结果状态，提升提取逻辑的可控性

**问题修复**
- 修复摘要草稿完成后通过模型注册表的路由保持
- 将数值工具参数限制在支持的整数范围内，防止非法值


### [jdx/mise v2026.8.6](https://github.com/jdx/mise/releases/tag/v2026.8.6) <sup>gpt-oss-120b</sup>

**新内容**
- 可恢复的 HTTP 下载，支持通过 Range 请求在验证 ETag 或 Last-Modified 时续传
- Homebrew cask 后端新增结构化 symlink 步骤、通用制品和复制/安装阶段，并支持事务回滚
- 任务运行 mise run --all 可打开交互式任务选择器，遍历整个 monorepo
- 新增 task.cache.audit_report，可将完整缓存审计报告写入 JSON Lines 文件
- dotfiles 条目支持 inline content 声明，便于小型配置和用户可写路径
- deps 提供者配置字段支持 Tera 模板渲染并进行环境变量展开
- 新增 config-root 范围的 [tool_config] locked = true 策略，强制工具使用锁文件
- 注册表新增 native-sdk、restate 二进制、mailpit 等工具

**Breaking Changes**
- mise use --global 与路径同时使用将被拒绝，不再默默忽略
- -b 现在是 --bump 的快捷方式，旧的 -l bump 缩写被隐藏并计划在 2027.8.5 移除

**问题修复**
- 修复 monorepo 模式下任务名称解析及工作目录规范化等问题
- 修复 HTTP 下载重试逻辑，包括 HTTP/2 REFUSED_STREAM 和系统安装路径
- 修复 Windows 平台下的激活、补全、Shell 路径、shim 名称及工具路径处理错误
- 修复 checksum 校验、pipx wheel 发现、conda 锁数据、npm 生命周期日志、OCI 标签处理等错误
- 修复配置编辑过滤、生成绝对本地化目录、锁文件包含任务特定工具等问题
- 修复 aqua 校验错误、ruby-build CLI 支持、rust nightly 解析、semver 标记等其他问题


### [OthmanAdi/planning-with-files v3.10.1](https://github.com/OthmanAdi/planning-with-files/releases/tag/v3.10.1) <sup>gpt-oss-120b</sup>

**新内容**
- 安装文档区分直接 npm vendoring 与 Pi 自动路由
- README 使用终端风格 SVG 展示 /clear 对比并标注统计表
- 版本号提升工具在缺失 ClawHub 暂存时不再报错，仍可更新验证
- npm 包源代码与同步清单已对齐至 v3.10.0 的 20 脚本 tarball

**问题修复**
- Codex 在 Linux 与 macOS 上的 shell 钩子改用共享事件适配器…



**帮助我们改进**

对今日摘要质量有建议? 欢迎反馈！

- [反馈摘要质量](https://github.com/houzixiashanxiedaima/AstroBlog/issues/new)
- [评价今日日报](https://github.com/houzixiashanxiedaima/AstroBlog/issues/new)

您的反馈将帮助我们优化 AI Prompt,提升摘要准确性
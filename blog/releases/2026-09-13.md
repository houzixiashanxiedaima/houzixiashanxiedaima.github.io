---
title: GitHub Releases 日报 - 2026-09-13
description: 2026-09-13 GitHub Releases 日报：abue-ammar/tinycast、ImranR98/Obtainium、Homebrew/brew 等14个项目更新 | 新增剪贴板图像和 PDF 的可选 OCR 搜索功能 | 新增应用列表密度设置 | 新增 Python 资源自动化命令
author: Release Bot
pubDatetime: 2026-09-13T08:00:00.000Z
draft: false
tags: ["releases", "daily", "automation", "tinycast", "Obtainium", "brew", "oh-my-posh", "husi"]
category: 开源追踪
ogImage: /images/covers/cover-2026-09-13.webp
---
### [abue-ammar/tinycast v0.10.20](https://github.com/abue-ammar/tinycast/releases/tag/v0.10.20) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增剪贴板图像和 PDF 的可选 OCR 搜索功能
- 支持在启动器中直接访问窗口或菜单栏项目
- 为文件搜索增加预览窗格、类型筛选和操作菜单
- 允许将剪贴板条目拖拽到其他应用程序
- 支持在扩展命令中使用别名字段
- 新增自定义快速操作功能
- 优化原生操作菜单的分组与圆角样式
- 新增调色板背景透明度控制选项
- 支持通过界面大小设置缩放调色板
- 改进内联命令参数使用方向键导航的体验
- 支持在标签环中按 Escape 键逐级退出
- 支持跨显示器循环切换屏幕区域
- 优化菜单搜索按菜单分组，并将设置项与对应命令关联显示
- 增强 Node.js 运行时兼容性（包括 node:util、node-fetch、Buffer.compare 等）
- 支持在 Note 中展开代码片段
- 优化引导窗口尺寸以适配每个步骤
- 支持未加引号的日期格式保留空格
- 支持在权限面板中请求日历访问权限
- 支持在扩展运行时中接受 process.addListener
- 支持运行渲染异步组件的扩展命令
- 支持隐藏空图标槽位
- 支持右键关闭已打开的菜单
- 支持在思考可关闭时提供 None 作为推理努力选项
- 支持通过 K 从搜索结果中隐藏特定启动器结果
- 支持在重新唤起调色板时自动选中搜索框文本

**安全修复**
- 启用强化运行时并配置更新器身份验证

**问题修复**
- 修复大型文件选择时剪贴板捕获 CPU 占用过高的问题
- 修复 TCC 重置后无法恢复日历访问权限的问题
- 修复扩展中搜索栏下拉菜单渲染问题
- 修复 ntd 货币代码未正确解析为 TWD 的问题
- 完善 execa 和 undici 所需的 node:util shim
- 修复裸退格键未能回退到根搜索的问题
- 修复分离子进程输出读取未完成时的等待问题
- 修复语言包标签显示错误的问题


### [ImranR98/Obtainium v1.6.16](https://github.com/ImranR98/Obtainium/releases/tag/v1.6.16) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增应用列表密度设置
- 支持在单个 apkUrls 条目中配置多个 Split APK URL
- 使用设备配置文件请求解析 RuStore Split APK
- 在下载大小探测中汇总所有 Split APK 的大小
- 添加共享的最小更新年龄辅助函数，并在添加应用时遵循该设置
- 重构源/提供者结构和 UI 代码，重新设计版本检测（包括滚动版本标签）
- 添加带有本地运行器的模拟器端到端测试套件
- 全面优化 Android TV 的 UI/UX
- 改进 APKMirror 跟踪功能，支持发布页面、变更日志、大小和包 ID
- 全局持久化 ForgeJo token，移除仅限 GitHub 的设置
- HTML 源：从解析后的 HTML 属性中发现 URL
- 修复 Uptodown 下载并添加搜索功能
- 添加华为 AppGallery .ru 主机支持
- 升级依赖项并迁移至 Flutter 内置 Kotlin 和 material_ui，弃用旧版 Kotlin Gradle Plugin

**安全修复**
- 在安装前验证 APK 签名证书

**问题修复**
- 修复版本检测、GitLab APK 资产、应用目录恢复和 AGP 构建问题
- 修复 HTML 源链接提取以支持相对路径和带引号的 URL
- 从安装程序自身结果中检测外部安装完成状态
- 为 RuStore 外部源列表显示特定错误信息
- 跳过基于列表的源中年轻于最小更新年龄的发布
- 应用更新时刷新缓存的应用图标
- 修复 GitHub issue 模板
- 更新多种语言的翻译文件（pl, ja, ru, hu, bs）
- 调整 CI 辅助工作流
- 其他各种网络、下载/安装相关的改进及 Bug 修复


### [Homebrew/brew 7.0.0](https://github.com/Homebrew/brew/releases/tag/7.0.0) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增 Python 资源自动化命令
- 支持 registry 包覆盖
- bump-formula-pr 支持使用 revision 作为版本进行资源升级
- livecheck 真正使用引用的 livecheck 选项
- install steps 新增 {{arch}} 模板令牌
- services 默认使用规范的 Linux 服务标签
- Mark unsatisfied requirements in brew info
- info 在标题中标记已禁用/弃用的 cask
- bundle: install batched casks as casks
- Allow limiting total build-from-source dependents
- test-bot: add padded prefix output
- Support registry package overrides
- Add commands for Python resource automation
- Decouple macOS release names from support status
- Reuse macOS release aliases in cask audit
- Show red ⊘ for packages that can't be installed
- Maintain bottle coverage during macOS bootstrap
- Promote macOS Golden Gate 27 to full support
- Report tiers and refresh stale bottle metadata

**安全修复**
- patch: harden staged-tree target containment in
- Narrow mkdir_p sandbox write permissions in
- Harden advisory history walks in
- advisory-match: protect reviewed ranges in
- curl: only expand secrets the download declared in
- Ban command literals in Homebrew source in
- Reject path components in internal command names in
- trust: ignore a bare tap name in ARGV in
- trust: normalise the installed-tap reference in
- cask/quarantine: gate metadata operations behind a single doorway in
- pypi: run metadata inspection in the sandbox in
- Fix SSH Python resource updates in the sandbox in
- Refine macOS sandbox compatibility in
- Adjust macOS subprocess policy in
- Escape dots in domain regexps in

**Breaking Changes**
- 将最低 macOS 要求提升至 Big Sur (11)
- 移除 Options merge 方法中的 hash 支持
- Strategy: 弃用 user_agent 回退机制
- Deprecate/disable/remove for major/minor release in
- Even more deprecations in
- Make macOS installer package Apple Silicon only in

**问题修复**
- 修复 brew edit 在未安装 tap 时的崩溃问题
- 修复 uses_from_macos cop 崩溃问题
- 修复 tap 打印无效名称而非崩溃的问题
- 修复临时服务 plist 后缀问题
- 修复 curl_download_strategy 缓存边缘情况
- 修复 diagnostic 缩进不一致问题
- 软化 doctor 输出问题清单措辞
- 修复 Formula-Cookbook 中的断链
- 清理无效的 UTF-8 重定位诊断信息
- 更新 json 至 3.0.1
- 澄清包提交压缩说明
- 若干琐碎修复（包括 RuboCop 配置、Sorbet 类型检查、Cask 审计正则表达式锚定等）


### [JanDeDobbeleer/oh-my-posh v31.3.0](https://github.com/JanDeDobbeleer/oh-my-posh/releases/tag/v31.3.0) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增 Pear 桌面环境分段

**问题修复**
- 修复 fish shell 中回车键处理器将空缓冲区视为无效的问题
- 修复 language 模块无法从 YAML 配置读取自定义工具的问题
- 修复 shell 和 kubectl 在映射或别名值中丢失标记锚点的问题


### [xchacha20-poly1305/husi v2.1.2](https://github.com/xchacha20-poly1305/husi/releases/tag/v2.1.2) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 支持设置多个 DNS 服务器时的 DNS 竞争
- JSON 编辑器：引号、键和字符串值使用不同颜色显示
- Android 端新增自动更新功能

**Breaking Changes**
- 移除 Tun IP 堆栈选项

**问题修复**
- 若干修复与改进（感谢 @grill-glitch）


### [dani-garcia/vaultwarden 1.37.3](https://github.com/dani-garcia/vaultwarden/releases/tag/1.37.3) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增 SSO_SIGNUPS_ALLOWED 配置项
- 新增 pm-32413-multi-client-password-management 功能标志
- 支持管理员重置双因素认证（2FA）
- 在双因素邮箱登录凭证失败时记录 IP 和用户名
- 将服务客户端路由至共享 HTTP 设置
- 更新 Rust Docker 版本及调整 DockerSettings
- log_event 函数参数由 i32 改为枚举类型
- 尽可能使用 insert_into 优化数据库操作

**安全修复**
- 修复安全漏洞：当凭证或 2FA 变更时撤销 2FA remember tokens
- 修复安全漏洞：对 prelogin 和 auth request 端点实施速率限制

**问题修复**
- 修复新版 web-vault 中密码修改问题
- 修复 MariaDB 12.2.2 的迁移问题
- 修复组织导入因缺少 groups 字段而失败的问题
- 修复 iOS 注册 token 响应问题
- 修复 archiveDate 更新问题
- 邮件禁用时忽略 reset-password 自动注册
- 修正 .dockerignore 中的无效注释语法
- 移除 ciphers.rs 注释中重复的 "the"


### [alyssaxuu/screenity v4.6.9](https://github.com/alyssaxuu/screenity/releases/tag/v4.6.9) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增录制冲突提醒：若上次录制未保存且编辑器仍打开，创建新录制时会发出警告并保留原录制在内存中
- 改进云录制的恢复机制，改为后台自动上传而非下载文件

**问题修复**
- 修复暂停后立即开始新录制时云录制变为静音的问题
- 修复窗口或屏幕尺寸变化导致视频中途出现花屏的问题


### [OthmanAdi/planning-with-files v3.18.0](https://github.com/OthmanAdi/planning-with-files/releases/tag/v3.18.0) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增通过 sh scripts/set-active-plan 或 PowerShell 脚本列出已保存命名计划的功能
- 列表输出显示每个计划的 ID、阶段计数及共享默认指针，便于会话前选择 PLAN_ID
- 优化阶段计数逻辑，支持混合状态格式、重复标记及六种语言模板，忽略代码示例与非相关章节
- 增强文件系统边界检查，确保目录、计划文件及指针读取符合当前项目范围
- 使用原子替换更新指针以保留硬链接文件并准确报告写入失败
- 在标准技能、插件脚本及 OpenCode、Mastracode、Hermes 等支持的 IDE 捆绑包中同步分发辅助工具与使用说明
- 明确 Kiro 的 .kiro/plan 布局单独文档化，且列表操作为只读，不自动选择计划或附加会话


### [exelban/stats v3.0.16](https://github.com/exelban/stats/releases/tag/v3.0.16) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增蓝牙系统信息缓存，避免每秒重复调用 system_profiler 和 pmset
- 授权后默认启用系统统计监控
- 新增超级核心压力测试功能
- 使用 needsDisplay 替代同步 display() 以优化小组件更新性能
- 替换预览图片为新版

**问题修复**
- 修复 NVMe SMART 温度在低于 273 K 时溢出的问题
- 修复无效历史计数或非有限值导致的图表崩溃风险
- 修复电池健康值超过 100% 的问题


### [rorkai/App-Store-Connect-CLI 5.3.0](https://github.com/rorkai/App-Store-Connect-CLI/releases/tag/5.3.0) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增 macOS PKG 导出、本地构建及预构建上传支持
- 新增手动 macOS 导出选项支持
- 强化 Xcode 归档输入与工具解析机制

**问题修复**
- 修复签名功能以支持原生 macOS profile 文件
- 修复发布流程在暂存前验证构建平台的问题
- 修复公证流程在上传前快照工件的问题
- 修复 Xcode 对不可变常规工件的验证问题
- 修复验证模块以确认显式版本所有权
- 修复公证流程中 Notary API 输入的验证问题
- 升级依赖（golang.org/x/crypto 0.56.0、golang.org/x/mod 0.41.0…


### [steipete/CodexBar v0.60.1](https://github.com/steipete/CodexBar/releases/tag/v0.60.1) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 优化 Codex 活动刷新性能，复用未变更的本地数据以减少重复验证与解码
- 提升账户卡片可靠性，确保身份、用量、积分及缓存成本正确关联至对应账户，并优先使用最新的订阅日期
- 优化提供商配额显示，区分 z.ai 不可用限制与未使用配额，移除 Antigravity 重复行同时保留不同限制

**问题修复**
- 修复 Vertex AI、Gemini、Claude 和 Antigravity 刷新凭据中特殊字符丢失的问题，并保留有用的 Antigravity 失败详情
- 修复 Token 账户在应用配置回退标签时丢失提供商账户 ID 的问题
- 修复 Grok 在无用量快照的账户卡片中错误显示缓存本地成本的问题
- 修复 z.ai 缺失配额限制时错误显示 100% 剩余的问题，保留真实零用量、计划详情及可选分析功能
- 修复 Antigravity 抑制完全镜像已知池的重置远程模型变体，保留不同配额行及其保存的可见性，优先使用已知用量而非仅重置的重复项
- 修复 OAuth 在 Gemini、Antigravity 和 Claude 刷新请求中保留保留字符的同时维持凭据所有权的问题
- 修复 Vertex AI 在 OAuth 刷新凭据中保留保留字符，并拒绝省略可用访问令牌的响应
- 修复 Antigravity 在不可用的 IDE 回退会用误导性的桌面启动指导替换尝试的 CLI 失败时的问题
- 修复 Grok 将 RPC 截止时间报告为超时，即使进程拆除立即关闭 stdout
- 修复 Codex 拒绝过大的数值支出限制重置时间戳而不崩溃，保留有效限制和用量
- 修复 Web 仪表板保留账户别名和组织标签，使共享电子邮件的 Claude 账户可区分，包括在脱敏模式下
- 修复提供商设置重新排序提供商时保留不可用插件的已保存配置，并一致地本地化 Qoder 和 Qwen Cloud cookie-cache 标签


### [jesseduffield/lazygit v0.65.1](https://github.com/jesseduffield/lazygit/releases/tag/v0.65.1) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 允许从压缩包运行测试

**问题修复**
- 修复部分终端上 Esc 键处理存在显著延迟的问题
- 避免在守护进程模式下加载或迁移用户配置


### [jdx/mise v2026.9.6](https://github.com/jdx/mise/releases/tag/v2026.9.6) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增实验性 [daemons] 配置与 mise daemons 命令族，支持通过 pitchfork 管理项目后台进程及 PostgreSQL/Redis …
- 新增 mise doctor project 命令，运行 [doctor.checks.] 中声明的环境诊断检查并报告结果
- mise search、shell completion 及交互式 mise use 现支持发现已安装 vfox 后端插件提供的工具
- 新增 settings.truncate 设置及 --no-truncate 参数，控制终端宽度截断行为，检测到编码代理时自动禁用
- HTTP 工具默认直接解压至独立安装目录，使 uninstall 和 prune 能彻底回收磁盘空间（可通过 shared_extraction = true…
- 优化 lockfile_mode = "generate" 下的热安装性能，跳过不必要的锁文件重写与构建
- OCI 构建、运行及推送共享本地工具层缓存，重复构建可跳过 tar 和 gzip 工作；新增 --no-cache 选项绕过缓存
- [bootstrap.macos.dock] 新增 apps 字段，支持有序固定应用路径并同步状态
- mise bootstrap packages brew: 现输出稳定 opt 根路径，便于脚本将 keg-only 可执行文件加入 PATH
- dotfiles 目标变体在均设置 target 时可省略 source，入口键解析为相对于 dotfiles.root 的路径
- mise fmt 现对无顺序意义的列表进行排序，包括 redactions 及任务源/输出等
- postgres、redis、mongodb 注册表项优先使用 conda: 后端以加速安装，vfox 和 asdf 作为回退
- 新增 mpv、agent-browser、himalaya 注册表项；修复 editorconfig-checker 和 mc 的安装问题
- 文档首页新增交互式图表，任务运行指南重新组织以提升可读性

**Breaking Changes**
- HTTP 工具默认安装布局变更：不再使用去重符号链接，而是直接解压至独立目录；需保留旧行为的用户须设置 shared_extraction = true

**问题修复**
- 修复 nushell 激活时在变量缺失时抛出 env_variable_not_found 错误的问题
- 修复从 run 条目引用的任务未自动安装任务级工具的问题，--skip-tools 现被正确遵守
- 修复 [monorepo] 设置在相同目录配置层间合并时的继承与覆盖逻辑错误
- 修复运行时工具请求（如 mise which hk --tool hk@latest）错误合并项目与全局 pin 导致歧义或选择错误 pin 的问题
- 修复 Packslip 工具完整锁文件生成时跳过无发布产物平台的问题，并允许验证的签名者替换旧元数据
- 修复 mise upgrade 无法检测带字母后缀版本（如 tmux 3.7b 到 3.7c）更新的问题
- 修复仅在 macOS 上因系统自带而 keg-only 的 formulae 在 Linux 上未链接到 /bin 的问题
- 修复带有 preflight_steps 或 postflight_steps 的 tap casks 在元数据提取期间失败的问题
- 修复 Windows 上 mise bootstrap remote 找不到 ssh.exe 和 tar.exe 的问题
- 修复 macOS defaults status 类型不匹配时显示两个看似相同值的问题


### [jxxghp/MoviePilot v3.0.1](https://github.com/jxxghp/MoviePilot/releases/tag/v3.0.1) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 音乐纳入完整自动化流程，支持搜索、订阅、下载、识别及整理，并对接 Navidrome、Emby 等媒体服务
- 统一媒体身份与多来源能力，以 media_source + media_id 贯通全流程，减少同名或跨来源误匹配
- 自动分类升级至版本化策略，提供可视化编辑、影响预览、历史回滚，且支持 category.yaml 自动迁移
- 任务可靠性增强，明确数据库事务边界，引入事件记录与恢复机制，降低重启时漏任务和状态不一致风险
- Agent 与插件体系增强，增加权限确认、异常恢复，插件支持虚拟分身、稳定 SDK 及 V2 兼容层
- 备份与运维扩展，支持数据库备份校验恢复，新增 AList、钉钉通知及独立服务状态入口
- 运行时升级至 Python 3.14，锁定依赖并引入 Rust 加速包，采用前后端分离发布链路
- 前端体验升级，新增音乐页面、分类编辑器、备份管理及响应式玻璃主题，保持 V2 导航习惯

**Breaking Changes**
- V3 数据库写入新结构后无法直接切回 V2 镜像，需使用升级前备份或官方降级流程



**帮助我们改进**

对今日摘要质量有建议? 欢迎反馈！

- [反馈摘要质量](https://github.com/houzixiashanxiedaima/AstroBlog/issues/new)
- [评价今日日报](https://github.com/houzixiashanxiedaima/AstroBlog/issues/new)

您的反馈将帮助我们优化 AI Prompt,提升摘要准确性
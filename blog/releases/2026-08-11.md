---
title: GitHub Releases 日报 - 2026-08-11
description: 2026-08-11 GitHub Releases 日报：callstack/agent-device、AChep/keyguard-app、nexu-io/open-design 等9个项目更新 | 新增 HarmonyOS 设备自动化支持，包括发现、快照、应用生命周期、HAP 部署等
author: Release Bot
pubDatetime: 2026-08-11T08:00:00.000Z
draft: false
tags: ["releases", "daily", "automation", "agent-device", "keyguard-app", "open-design", "brew", "karakeep"]
category: 开源追踪
ogImage: /images/covers/cover-2026-08-11.webp
---
## 2026-08-11 更新摘要

**统计信息**
- 由 gpt-oss-120b (8), Qwen/Qwen3.5-35B-A3B (1) 总结
- 监控仓库: 17 个
- 今日更新: 9 个项目 (9 个版本)
- 生成时间: 2026-08-12 05:05:30 (北京时间)


### [callstack/agent-device v0.20.7](https://github.com/callstack/agent-device/releases/tag/v0.20.7) <sup>gpt-oss-120b</sup>

**新内容**
- 新增 HarmonyOS 设备自动化支持，包括发现、快照、应用生命周期、HAP 部署等
- open --foreground 可直接附加到未歧义的 iOS 模拟器并立即返回交互式快照
- 常用命令启动速度提升约 54%，CLI 启动时间从约 146ms 降至 68ms
- snapshot --actions 支持在合并的可访问元素上暴露自定义操作
- 隐藏键盘文本输入在提交完整文本后才报告成功
- Android 快照恢复机制改进，自动退役不健康的辅助会话
- 掩码填充返回目标绑定的验证证据，免费文本 shell 参数安全引用
- 强化云端和远程设备支持，BrowserStack、AWS Device Farm iOS 会话可靠捕获快照，云填充等待焦点证据
- MCP 服务器兼容无状态 2026-07-28 修订版，支持发现和可缓存工具元数据
- 选择器在出现真正不同的歧义匹配时拒绝，而非静默选择
- CLI 帮助卡和版本头部紧凑展示
- 在 iOS 上暴露合并可访问元素的自定义 AX 操作
- 添加模拟器验证技能
- 将设备库存、日志、网络捕获迁移至基于请求的平台运行时，提升取消、恢复和进程重启处理
- 通过懒加载专用客户端命令处理器提升性能

**Breaking Changes**
- 选择器在出现真正不同的歧义匹配时拒绝，可能导致之前默认接受的用例需调整

**问题修复**
- 若干关键错误修复（daemon 提示、隐藏键盘提交、Android 快照可靠性、并发远程连接隔离、XCTestDevices 重定向、云端快照与填充…


### [AChep/keyguard-app r20260811](https://github.com/AChep/keyguard-app/releases/tag/r20260811) <sup>gpt-oss-120b</sup>

**新内容**
- 新增 GPG 密钥管理功能，可在 Keyguard 中导入、管理并设置密钥及子密钥的过期时间
- 在 Windows 上提供 SSH/GPG 代理，支持调用方范围的批准及 Wear OS GPG 项目类型
- 重构 KeePass (KDBX) 同步引擎，支持最高 500 MB 附件、流式编码并实现本地分段上传以提升可靠性
- 新增 Android 集成：OpenKeychain Provider、Credential Exchange (CXF) 以及在自动填充前验证目标应用的包名…
- UI/UX 改进：可自定义导航栏、链接 vault 项目、简化嵌套文件夹创建、智能移除已删除项的选择，并在桌面阻止 IME 输入
- 支持 WebDAV 远程 vault，改进与不支持原子写入的服务器的兼容性
- 性能优化：加速 vault 列表加载、搜索以及 Watchtower 扫描
- 更新内部数据库，包含最新的 passkey、双因素认证服务和公共域后缀列表

**安全修复**
- 在 macOS 与 Windows 上实现剪贴板保护，防止复制的密码被写入系统剪贴板历史
- 强化 KDBX 文件解析安全，限制 KDF 参数并校验内部结构以防止恶意数据库
- 对 Windows 临时文件设置安全权限，确保同步过程中的临时文件不被未授权访问
- 硬化 SSH/GPG 代理的密钥策略，在签名和解密时执行更严格的密钥和公钥验证

**问题修复**
- 修复 Flatpak 发行版缺失托盘图标的问题
- 改进 Bitwarden 同步，单个条目 JSON 解析错误不再导致整体同步失败
- Watchtower 现在能够标记仅出现一次的泄漏密码
- 修复加密存储失败的恢复机制，确保在特定错误下能够恢复数据
- 若干其他小错误修复，包括错误信息拼写、CI 稳定性等


### [nexu-io/open-design open-design-v0.19.0](https://github.com/nexu-io/open-design/releases/tag/open-design-v0.19.0) <sup>Qwen/Qwen3.5-35B-A3B</sup>

**新内容**
- 新增图像生成功能，支持 Seedream 5.0 Pro、GPT Image 2.0 和 Nano Banana 2.0
- 支持在生成时传递多张参考图及指定分辨率、质量等参数
- 视频生成集成 Vela 引擎
- 导出、分享与移交功能恢复为直接操作按钮
- HTML 与 React 组件查看器结构统一，修复离屏编辑器崩溃问题
- 共享项目同步优化，排除生成树并限制后台拉取大小
- MCP 客户端启动时可指定多个技能 ID
- 发布 Launch Week Vol.01 活动页面
- 请求时隐藏控制防止未打开内容泄露至源码
- 预览 iframe 失败产生受限的诊断事件
- Qwen Code 模型选择优先使用用户配置，内置模型作为回退
- 演讲者备注在演示视图中可滚动显示而非被裁剪
- 文件计数逻辑修正，统计唯一文件而非重复写入
- 设计系统可在非工作区环境下删除
- 桌面端更新后刷新持久化启动器路径

**问题修复**
- 修复生成中断后的结果丢失问题，支持自动恢复
- 修复工作区上下文临时故障不重用旧账户状态的问题
- 修复对话分支边界错误及 MCP 项目操作绑定问题
- 修复旧重试任务关闭新生成任务的并发问题
- 修复设计运行状态在重载时重复附加的问题
- 修复工作区依赖写入时的权限失败重试逻辑
- 修复工作区错误诊断代码信息不足的问题
- 修复新项目评论不再复用已退役的 Pin 编号
- 修复邀请对话框因过期计时器意外关闭的问题
- 修复 Claude Enterprise 认证在运行时检测中的识别问题
- 修复 MCP 项目工具在工作区升级后返回空目录的问题
- 修复 daemon 静态根配置下直接导航应用路由失败的问题
- 修复 MCP 客户端重启导致桌面循环重启的问题
- 修复本地代理在套接字耗尽时重复失败请求的问题
- 修复 Windows 卸载仅移除当前安装协议注册及 NSIS 路径引号问题
- 修复诊断导出中日志记录与 JSONL 事件尾对齐问题
- 修复 deck 预览在加载到隐藏浏览器标签页后无法恢复的问题
- 修复删除项目后 Design Browser 历史记录和视口缓存未清除的问题


### [Homebrew/brew 6.0.17](https://github.com/Homebrew/brew/releases/tag/6.0.17) <sup>gpt-oss-120b</sup>

**新内容**
- 新增 SBOM 构建，使用 Vulns::Purl 生成 brew 包的 purl
- 新增报告跳过的依赖升级
- 允许 cask 运行时访问网络


### [karakeep-app/karakeep v0.33.2](https://github.com/karakeep-app/karakeep/releases/tag/v0.33.2) <sup>gpt-oss-120b</sup>

**新内容**
- 支持通过 CLI 上传自定义 SingleFile 档案
- favicon 适配浏览器亮暗模式
- 迁移至自定义 karakeep Chrome 容器（新容器已发布）
- iOS 端使用上下文菜单

**问题修复**
- 默认 OpenAI 配置下将 “use max completion tokens” 环境变量设为 true
- 大型 SingleFile 档案不再因内联视频导致 OOM，已剥离内联视频
- 加固爬虫链接转资产机制，防止资产静默损坏
- 将 Node.js 锁定至 24.18.1，规避 24.19 版本的 bug
- 修复 better-sqlite3 与 Node 产生的崩溃循环
- 爬虫下载 URL 失败时抛出错误
- restate 改用 logger 中间件替代 durable 步骤
- workers 在元数据解析前删除内联媒体块


### [mihomo-party-org/clash-party v2.0.1](https://github.com/mihomo-party-org/clash-party/releases/tag/v2.0.1) <sup>gpt-oss-120b</sup>

**新内容**
- 优化冷启动关键路径，首屏路由按需加载，渲染后再初始化运行文件、内核、流量监控和 Sub-Store，降低启动资源争用
- 优化退出清理流程，核心进程、DNS 和系统代理采用有界清理，避免慢 helper 或子进程阻塞退出
- 支持按配置跳过启动前的内核配置检查，缩短特定场景下的内核启动时间
- 升级项目依赖并迁移至 pnpm 11

**问题修复**
- 若干修复（Linux Wayland 复制环境变量、内核启动/重启死锁、内核异常退出恢复、主窗口渲染白屏、Sub-Store 服务启动…


### [steipete/CodexBar v0.49.2](https://github.com/steipete/CodexBar/releases/tag/v0.49.2) <sup>gpt-oss-120b</sup>

**问题修复**
- 合并同一提供商账户的重复使用行，保留当前 Mac 的配置结果优先。
- 菜单卡：在每周/每月计划的最终重置前显示配额已用尽，同时保留会话细节和独立购买的积分。
- 菜单卡：当主要使用值和本地化重置时间戳无法同时显示时，堆叠行以保持可读性。
- 插件：在用户安装的提供商卡片中遵循“使用条填充”剩余/已用设置，保持百分比标签和条方向一致。
- Sub2API：将配额标签、本次请求、令牌和费用总计本地化并分组为紧凑的使用摘要。
- Codex：避免在成本缓存刷新期间重复转换历史令牌快照，防止大历史会话的持续 CPU 占用。
- Codex：将自动成本历史追赶设为近空闲，并将本地历史扫描限制为提供商刷新，每 15 分钟一次。
- Agent Sessions：在监控被禁用时停止不活跃的本地和远程刷新调度器唤醒。
- 成本使用：在“打开菜单时刷新所有提供商”启用时刷新令牌费用数据，并设定一分钟扫描底线以避免重复工作。


### [jdx/mise v2026.8.4](https://github.com/jdx/mise/releases/tag/v2026.8.4) <sup>gpt-oss-120b</sup>

**新内容**
- 后端工具可声明显式 version_order，实现语义化版本排序（Aqua、GitHub、GitLab、Forgejo、HTTP）
- mise ls 现在能够匹配跨后端安装的同一工具
- mise install --force 可在不指定工具时重新安装所有已配置、支持的工具
- 新增 upgrade.auto_prune 设置（默认 true）并提供 --prune 标志，控制升级后是否自动删除旧版本
- bootstrap packages prune --manager brew-cask 可保守地移除不再声明的 Homebrew cask
- bootstrap.packages 支持表格形式，可指定版本和操作系统选择器，实现跨平台包管理
- brew-cask 管理器支持从 Git URL 安装字体 cask，可指定分支和子目录
- aqua 相对 registries 条目现在相对于配置根解析，便于项目内部 registry.yaml 使用
- spm 安装可通过 rev（或 ref）固定到特定提交，支持源码构建
- generate 生成的 git hook 可携带额外的 mise 标志，支持在子目录或全局标志下运行
- watch 在未指定任务名时默认运行默认任务，避免错误
- activate 的 --silent 与 --log-level 现在传递至生成的激活 hook，支持静默输出
- sops 解密在未提供 age key 时不再被跳过，支持 KMS、Vault、PGP 等非严格模式
- Windows 平台上 conda、ruby、rust 等工具安装行为改进，解决 DLL 丢失、版本选择和并发冲突等问题
- 任务系统改进：Azure DevOps 远程 git 包含处理、单仓库任务匹配、任务输出检测、参数验证及 hook 配置解析

**问题修复**
- Windows 平台上 conda DLL 放置、RubyInstaller2 构建选择、Rust 工具链并发安装序列化等问题已修复
- 任务相关修复：Azure DevOps 远程 git 包含、单仓库任务匹配、任务输出检测与参数验证、hook 配置解析等
- 其他修复：watch 默认任务、activate 静默日志转发、sops 解密、bootstrap 依赖顺序、registry URL、cli 版本前缀解析…


### [localsend/localsend v1.18.0](https://github.com/localsend/localsend/releases/tag/v1.18.0) <sup>gpt-oss-120b</sup>

**新内容**
- 首次发布 CLI
- 支持通过链接接收文件
- 自定义配色主题（颜色选择器）
- 支持 IPv6
- 默认启用校验和
- 默认自动接受收藏设备的文件
- 使用十进制单位显示文件大小（1 GB = 1000 MB）
- 支持使用 --text 或 -t 参数启动文本分享
- 改进长时间传输的剩余时间显示
- 更改重复文件命名格式为 "file (1).txt"
- Android 后台传输并添加快速设置磁贴
- macOS 多项改进：快捷键、按钮、ComputerName、Dock 图标文字拖放
- Linux 使用原生窗口装饰取代 GTK3 大标题栏
- 性能提升：对慢速接收设备更快传输
- Windows 将 LocalSend 添加到共享面板

**问题修复**
- 修复大量文件收发导致的卡顿/延迟
- 传输完成后释放唤醒锁，允许设备进入睡眠
- 修复历史对话框中文本消息显示三次及大小计算错误
- 修复 DNG 文件保存到相册的问题
- 修复 iOS/Android 将不支持的媒体格式保存到文件夹的错误
- 修复 Android 分享到 LocalSend 有时失效并保留位置元数据
- 修复 macOS 启动隐藏时 Dock 图标短暂出现以及拖拽/共享扩展恢复
- 修复 Linux 添加 CJK 字体支持



## 今日播客更新

- **得体广播站**: [Ep45.我是一只无法压抑的自由小鸟](https://www.xiaoyuzhoufm.com/episode/6a7a0c0b36641f136d862e74?utm_source=rss)

**帮助我们改进**

对今日摘要质量有建议? 欢迎反馈！

- [反馈摘要质量](https://github.com/houzixiashanxiedaima/AstroBlog/issues/new)
- [评价今日日报](https://github.com/houzixiashanxiedaima/AstroBlog/issues/new)

您的反馈将帮助我们优化 AI Prompt,提升摘要准确性
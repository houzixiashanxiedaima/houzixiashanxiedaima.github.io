---
title: GitHub Releases 日报 - 2026-08-29
description: 2026-08-29 GitHub Releases 日报：rorkai/App-Store-Connect-CLI、EKKOLearnAI/hermes-studio、ImranR98/Obtainium 等13个项目更新 | 新增深度提交验证功能 | 统一 Studio 代理运行时管理
author: Release Bot
pubDatetime: 2026-08-29T08:00:00.000Z
draft: false
tags: ["releases", "daily", "automation", "App-Store-Connect-CLI", "hermes-studio", "Obtainium", "pi", "executor"]
category: 开源追踪
ogImage: /images/covers/cover-2026-08-29.webp
---
## 2026-08-29 更新摘要

**统计信息**
- 由 Qwen/Qwen3.8-27B (12), Qwen/Qwen3.5-27B (1) 总结
- 监控仓库: 21 个
- 今日更新: 11 个项目 (13 个版本)
- 生成时间: 2026-08-30 20:26:19 (北京时间)


### [rorkai/App-Store-Connect-CLI 4.11.0](https://github.com/rorkai/App-Store-Connect-CLI/releases/tag/4.11.0) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增深度提交验证功能
- 应用墙新增 TapTape 和 Somilo

**问题修复**
- 修复冷 Go 缓存下帮助输出保留问题
- 优化 Slack 通知验证诊断信息的简洁性
- 修复遥测中 snitch 描述分类为叶子调用的问题
- 升级依赖（aws-sdk-go-v2、smithy-go、credentials、s3、go-utils/v2）


### [EKKOLearnAI/hermes-studio hermes-0.20.6-runtime - v0.7.1](https://github.com/EKKOLearnAI/hermes-studio/releases) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 统一 Studio 代理运行时管理
- Ekko 新增托管内存、技能、MCP 及设置支持
- Ekko 支持并行工具调用
- Ekko 支持全局代理配置
- 将独立 Ekko Agent 整合至 Studio
- 优化附件传输与媒体预览体验
- 加速 Agents 状态加载速度
- 桌面运行时默认版本设为 Hermes 0.20.6
- 更新 Hermes Studio README 架构文档
- 发布 Hermes Studio 桌面运行时，打包上游 Hermes Agent 0.20.6

**Breaking Changes**
- 保留旧版 MCU API 路由以维持兼容性

**问题修复**
- 修复内存持久化机制，使其更直接且可恢复
- 修复聊天中导航时思考耗时丢失问题
- 修复编码代理运行期间工具调用丢失问题
- 修复最近列表分类折叠状态在刷新后丢失问题
- 修复聊天中个人资料和代理身份显示异常
- 加固 Ekko 内存审查与工具失败处理机制
- 修复 Studio 拥有的进程树关闭清理逻辑
- 修复 Codex 设置所有权、代理辅助及运行时打包问题
- 恢复无效的 Ekko 启动配置
- 隐藏全局编码代理推理力度控制
- 明确 Weixin 绑定通知限制说明
- 保留停止后的全局编码代理上下文
- 剥离历史内联图片
- 隔离 Ekko 开发数据
- 强制规范服务器模块所有权


### [ImranR98/Obtainium v1.6.14](https://github.com/ImranR98/Obtainium/releases/tag/v1.6.14) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增对第三方 F-Droid 仓库的 index-v2.json 支持
- 新增针对单个应用的最小更新年龄滑块，支持“使用全局默认值”和“无延迟”选项
- 优化分类按钮流程，使其与分类标签保持一致
- 将 README 指向新的 Deep Links wiki 页面

**问题修复**
- 修复 Forgejo 私有覆盖主机支持问题
- 修复 RuStore 子域名证书固定被忽略的问题
- 修复批量更新时安装确认与轮询竞争导致挂起的问题
- 错开失败应用的后台更新重试间隔
- 修复 F-Droid 仓库搜索未填充所选应用 ID 的 bug
- 修复滑块表单项缺少必需参数及 const lint 问题
- 修复移除 appdl 回退后华为 AppGallery 应用 ID 推断错误的问题
- 停止在华为 AppGallery API 失败时使用 APK 文件名时间戳作为版本
- 显示真实的 GitHub token 拒绝错误，并在 PAT 被拒时回退到未认证请求
- 修复编辑分类表单项操作按钮被系统导航栏遮挡的问题
- 更新 zh、ru、nl、hu、ja 等语言的翻译文件


### [earendil-works/pi v0.84.4](https://github.com/earendil-works/pi/releases/tag/v0.84.4) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增终端能力覆盖功能，可覆盖自动检测的超链接、图像和真彩色支持
- 新增扩展 UI 提示事件（ui_prompt_start/end），便于区分代理活跃工作与等待用户输入的时间
- 新增 RPC clear_queue 接口，用于获取并清除队列中的转向及后续消息
- 新增全屏选择复制控制，可禁用自动复制并使用 Ctrl+X 复制当前选区
- 新增实验性 DeepSeek V4 Flash Vision 视觉模型支持
- 公开库导出中新增 detectSupportedImageMimeTypeFromFile() 函数
- 启用缓存未命中通知时，压缩与分支摘要现在会显示转录用量提示

**问题修复**
- 修复切换思考可见性时清空正在运行的 Bash 工具部分输出的问题
- 修复 Windows 下 taskkill.exe 不在 PATH 中导致 shell 中止崩溃的问题
- 修复恢复会话时若 JSONL 文件缺少末尾换行符导致下一条追加条目损坏的问题
- 修复代理运行时发送 triggerTurn: false 的扩展消息被插入到工具调用及其结果之间的问题
- 修复压缩和分支摘要强制设置 toolChoice: "none" 的问题
- 修复大型工具结果超过自动压缩阈值时先发送给提供商的问题，现改为在工具执行后、下一次助手响应前进行压缩
- 修复使用 HTTP(S) 代理时 Google Vertex 请求因 HttpsProxyAgent 报错失败的问题
- 修复从非空模型作用域保存默认模型后在该作用域不可用的问题
- 修复继承的 @ 文件自动补全排序偏好直接和较浅匹配而非嵌套路径的问题
- 修复继承的 OpenAI 兼容流在流式传输期间重复序列化思考签名的问题
- 修复继承的主屏幕渲染在图像密集输出超过 V8 字符串长度限制时崩溃的问题
- 修复继承的全屏双击词选择在 / 和 - 处错误分割路径和短横线命名法标记的问题
- 修复继承的 Cloudflare AI Gateway 目录遗漏支持的 workers-ai/* 直通模型的问题
- 修复继承的 OpenAI 兼容推理重放合并连续流式文本和摘要 reasoning_details 增量
- 修复继承的 OpenRouter 推理控制使必须推理的模型接收 effort: "none" 的问题
- 修复继承的 OpenAI 兼容 Chat Completions 在未定义工具时忽略显式请求的 toolChoice 的问题
- 修复继承的分片 Mistral 工具调用在续块省略工具调用 ID 时被拆分的问题


### [UsefulSoftwareCo/executor v1.6.3](https://github.com/UsefulSoftwareCo/executor/releases/tag/v1.6.3) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 工具目录刷新改为并发执行，避免串行延迟叠加
- 新增 EXECUTOR_TOOLS_SYNC_TTL_MS 环境变量以配置自托管环境的同步新鲜度窗口
- 支持通过设置 off/null/false 禁用基于时间的重新同步
- 插件可等待事务提交后再执行不可逆清理操作
- MCP 作用域发现不再依赖应用资源，回退至集成自身的发现 URL
- OAuth 令牌刷新时保留原始授权请求中的 resource 参数
- 升级依赖包（@executor-js/local、sdk、api、runtime-quickjs）

**安全修复**
- 修复 OAuth 令牌端点错误消息中泄露访问令牌和刷新令牌的问题
- 采用白名单机制过滤错误预览字段，防止意外敏感信息暴露
- 移除失败对象中对底层拒绝原因的引用，防止原始令牌数据被渲染
- 在 OpenAPI 健康检查中按键名和值双重路径脱敏凭证信息
- 修复删除客户端时凭据提供者未参与事务导致密钥永久丢失的问题

**问题修复**
- 修复 SQLite 列类型变更导致旧格式 OAuth 过期时间无法读取而隐藏所有集成的问题
- 修复工具目录重建失败时静默无日志的问题，现记录警告及原因
- 拒绝启动时传入非法的 EXECUTOR_TOOLS_SYNC_TTL_MS 值而非静默回退默认值


### [nicobailon/pi-subagents v0.59.0](https://github.com/nicobailon/pi-subagents/releases/tag/v0.59.0) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增 runs.host(...) 命令步骤，支持超时、保存输出及状态回执
- 新增 CI 和门禁监控行，记录监控类型、终端判定及报告指针
- 持久化工作流通道元数据至子状态、回执及 worktree 交接清单
- 新增 runs.lanes(...) 支持并行顺序工作流阶段及保留恢复
- 新增预检通道元数据，在启动、状态及实时卡片视图中渲染计划通道
- 新增 plan-only worktree.cleanup 管理操作，仅记录清理计划而不删除资源
- 为每个子代理会话提供人类可读显示名称，并包含在各类负载中
- 新增 /subagents-steer 命令，允许非 TUI 会话和 RPC 主机引导异步运行
- 新增 allowNestedSubagents 授权，支持嵌套扇出而不替换继承工具
- 支持通过 /subagents-stop 指定子 ID 停止多子运行中的单个子任务
- 减少启动、状态、输出及 worktree 清理的冗余消息，保持行为不变
- 在缺失的工作流回执指导中包含可直接恢复的子 ID
- 明确工作流脚本契约中关于保留恢复键和持久化子输出的定义
- 在 worktree 交接清单中记录合并或取代证据，显示清理资格
- 在异步状态行中显示可选的通道和工作项上下文（如阶段、门禁等）
- 在异步状态进度中显示工作流子标签和阶段，同时保持稳定的工作流键
- 移除助手轮次预算，包括强制终止、收尾提示注入及启动配置
- 将内部启动、状态、结算等代码拆分为更小的模块，不改变公开行为

**问题修复**
- 修复分离式工作流完成匹配逻辑，保持主机门禁行快照并报告缺失判定
- 原子性替换 runs.host(...) 输出文件，并重试 Windows 临时锁定
- 拒绝加载 JSON 时格式错误的 MCP 直接工具服务器及元数据缓存字段
- 保留 Cursor 原生实时复合子工具调用 ID，确保 MCP 结果匹配待执行项
- 忽略后台状态刷新和会话生命周期清理期间的过期缓存 UI 上下文
- 压缩默认 TUI 和状态视图中的工作流预检状态，展开时保留完整细节
- 为 runs.lanes(...) 阶段 0 保留恢复验证提供可操作的 runs.run(...) 指导
- 从 TUI 操作员面向的异步状态行中移除剩余的 lane: 前缀
- 允许通过注册 Git worktree 的 .pi 符号链接共享计划项目根目录
- 在子代理工具结果和 /subagent-cost 中包含委托子使用量及父压缩使用量
- 避免对生成的 runs.lanes(...) 阶段键产生虚假的预检不匹配警告
- 接受工作流子摘要中的长主机工具调用 ID，匹配现有 4,096 字节限制
- 保持异步工作流脚本续接活跃，以便后续顺序步骤在子协调期间仍能运行
- 防止过期的扩展和斜杠命令上下文在重新加载或会话替换期间逃逸
- 在完成通知中包含保存的工作流子输出路径和内联预览
- 将百万级上下文限制格式化为 1M 而非 1000k 以优化实时状态显示
- 在重新加载后于 Fleet Status 下恢复活动工作流子节点，保留未匹配的 shell 行
- 在异步 TUI 通道行中优先使用已加载的工作区上下文而非内部工作流键
- 接受长达 4,096 个字符的路径型 Pi 会话 ID 用于快照后台工作
- 接受提供者驱动程序报告的裸叶模型 ID 以验证提供者限定的启动候选项
- 当保留状态安全时，一次性恢复因压缩导致的子中止，否则报告确切阻碍因素
- 在缺失输出交接诊断前，报告被中止或信号无输出子运行的终端停止或 stderr
- 将 globalConcurrencyLimit 应用于通过 runs.run 和 runs.all 启动的工作流子节点
- 在代理发现期间忽略嵌套的 .pi 和 sync-backups 目录，防止陈旧备份成为可执行代理
- 允许项目层根据活动父模型提供者分层覆盖代理，无需重复定义
- 允许操作员配置默认 subagent_wait 窗口，并将窗口过期报告为非错误活动工作
- 当持久化状态不可用时，将运行状态降级为存储的扇出预算快照而非失败整个列表
- 强制执行 MCP 服务器 includeTools 和 excludeTools 策略用于子直接工具授权
- 防止 Fleet 提示审计渲染在处理格式错误的非字符串任务负载时崩溃
- 在提供者或传输中止后有用进度之后，一次性恢复保留的子会话而不重启任务
- 在提供者报告纯文本 500 或内部服务器错误后重试未使用的回退模型
- 将具有有用变更证据的缺失必需子交接标记为部分需关注结果


### [nicobailon/pi-web-access v0.27.0](https://github.com/nicobailon/pi-web-access/releases/tag/v0.27.0) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增 fetch.timeout 配置项，允许在 web-search.json 中设置直接 HTTP 和 Jina Reader 的超时时间
- 支持为 answer mode 配置默认的 provider 和 model，同时保留单次调用时的覆盖能力

**问题修复**
- 修复相对 canonical 链接页面解析时出现的 ERR_INVALID_URL 警告问题
- 隔离不同 Pi 运行时下的 GitHub clone 工作目录，防止并发进程间相互干扰导致清理失败


### [CherryHQ/cherry-studio v2.0.10](https://github.com/CherryHQ/cherry-studio/releases/tag/v2.0.10) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增定时任务卡片运行状态显示
- 新增模型抽屉 Token 限制预设
- 新增 Code Mate Hermes 仪表盘
- 支持 Markdown 导出时携带多模态图片
- 新增 ECharts 图表在 Markdown 代码块中的渲染
- 新增 Artifact Pane 文件头部右键菜单
- 提升聊天界面 Token 用量可见性
- 新增 Parallel 搜索提供商
- 同步 CLI 技能与已安装工具
- 新增机械式错误日志扫描诊断功能
- 支持 Shift+点击范围选择文件
- 新增空笔记页面创建操作
- 引导初始 API 设置流程
- 支持拖放文件上传至知识库
- 默认 Agent 优先展示给新用户
- 提交签名诊断报告至 Cherry
- 导出聊天记录至诊断包
- 修复开机自启功能并添加测试
- 支持知识文档拖放上传

**安全修复**
- HTML 预览 iframe 增加同意门控，沙箱默认值失败关闭

**问题修复**
- 修复 Markdown 自动链接吞没强调标记问题
- 修复拼写检查实时高亮和多语言问题
- 优化子代理流程和运行状态
- 并行化独立配置管道以提升性能
- 注册便携式 Windows 启动器登录项
- 优化技能图标样式
- 锚定相对日期到运行时
- 防止嵌套输入触发键盘拖拽
- 保留快速消息导航目标
- 并行化设备元数据读取以提升备份性能
- 稳定系统提示词前缀以优化代理性能
- 清理孤立标签池状态
- 暴露聊天补全中的缓存 Token
- 每次启动仅跟踪一次应用启动分析
- 调整 Markdown 渲染节奏并将一次性高亮移至 Worker
- 路由 MiniMax M3 代理通过 Anthropic Messages
- 阻止纯文本模型读取图像
- 容忍 OpenAI Responses 回复中缺失的注释
- 填充代码表面内的 Composer 内边距
- 保持自定义操作页脚可见
- 解耦链接芯片颜色与用户主题
- 更新分析 SDK 并过滤 Token 使用报告
- 从一致快照重新对齐标签池当前状态
- 条件发送 includeServerSideToolInvocations 用于 Gemini 3 工具组合
- 修复不完整 DSH 安装
- 命名自定义参数值输入
- 从 Composer 输入中过滤子菜单面板
- 将拖拽标签保持在安全边界内
- 延迟加载代理工具渲染器以优化聊天性能
- 始终在消息页脚显示 Token 用量
- 为 Kimi 模型省略固定 top_p
- 规范化侧边栏图标尺寸
- 延迟加载非活动面板以优化数据设置性能
- 缩小裸代理关键词范围
- 将系统提示词映射到 OpenAI Responses API 的 instructions 字段
- 保留 Copilot 必需请求头
- 标准化平台资产文件名
- 升级 pi-ai 至 0.80.6 以支持压缩输出预算
- 覆盖外部重定向期间的传出页面
- 通过 Cherry fetch 路由提供商请求
- 稳定消息列表中的键盘滚动
- 定位消息后保持 Token 详情关闭
- 换行预览编辑模式中的长行
- 接受仅标记为 general 的 SearXNG 引擎
- 在纯文本复制中保留数学公式
- 使过期的 Electron 元数据失效
- 保留依赖作用域注册以优化 Composer 工具
- 为辅助技术命名列表位置控件
- 本地化元工具卡片部分标题和空状态
- 隐藏检查更新错误中的原始 503 响应体
- 处理结构化查询和缺失限制
- 恢复传统工具步骤顺序
- 仅水合可见视图以优化文件性能
- 转发 azure-anthropic 变体的代理获取
- 限制超宽窗口上的仪表板内容宽度
- 向 Pi Bash 暴露托管工具链
- 一致显示提供商名称
- 在重新生成时尊重当前模型
- 提供 DSH 工具拒绝反馈
- 不区分大小写合并请求头
- 将最大 Token 长度截断作为错误而非成功显示
- 为辅助技术命名 GitHub 和 Logo 控件
- 处理 HuggingFace 模型并清理工具描述
- 支持 GLM-5.3-Flash 推理
- 发布完整的远程目录
- 将交付范围限定于可信目标
- 刷新会话/主题列表的时间桶引用
- 保留带有 data 参数的文件日志行的源元数据
- 保留便携式版本重启后的提供商资源
- 处理 Gemini MCP 数组工具架构
- 显示活动权限模式
- 强制转换 pi 和 dsh 的提供商额外请求头
- 在模型标签中显示提供商名称
- 延迟加载可选菜单工作流以优化笔记性能
- 使用所选语言进行诊断输出
- 简化发布验证流程
- 仅在根搜索中显示技能后缀
- 在安全重定向上保留身份验证
- 安全计数字面特殊 Token 文本
- 本地化日志和编辑对话框关闭标签
- 隔离标签状态副作用以优化主页和代理性能
- 接受现有的绝对 MCP stdio 命令
- 拒绝空文档提取
- 保留多图像输入
- 将 Hermes 配置写入实际读取的主目录
- 确保计划频道结果仅交付一次
- 修复推荐标签链接
- 稳定虚拟化视图回调以优化文件性能


### [steipete/CodexBar v0.56.0](https://github.com/steipete/CodexBar/releases/tag/v0.56.0) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- Antigravity：支持直接从本地 SQLite 对话中追踪 token 历史，无需 tokscale 导出；不可用的历史与零用量保持区分…
- Cursor：当用量事件缺少价格时，使用缓存或内置定价估算 API 速率成本，未知成本保持未定价，且估算值与 Cursor 计量费用分开显示

**安全修复**
- CLI 安装：将辅助验证和管理员命令从继承的 shell 函数和启动钩子中隔离，同时保留现有审批流程和安装位置（#3205）

**问题修复**
- Cursor：修复空成本历史窗口显示解码错误的问题，同时保留不完整历史和格式错误响应检查
- Codex：尊重原生访问令牌的过期时间，使有效 OAuth 会话保持模型特定限制，凭证刷新仍由 CLI 负责（#3221）
- Codex：成功获取后清除过期的连接错误，即使周用量尚无法发布，包括持久化和堆叠账户错误
- Grok：在计费期间原生登录变更时，保持用量、身份和计划在同一账户上；基于 cookie 的用量不再拾取无关的 auth-file 元数据
- Codex：防止过期的 OpenAI 仪表板准备和超时重试替换替代视图，并在视图离开缓存时保留清理逻辑
- Usage & Spend：常规用量更新后保持 365 天历史新鲜，不丢失旧行或不启动重叠扫描
- Overview：当另一个选定的订阅没有支出数据时，保留已知历史日覆盖范围，同时保持不完整总计标记为部分
- Codex：全局成本跟踪关闭时刷新本地会话成本估算，而不是反复拒绝成功的扫描为过期
- Codex：在繁忙的成本扫描期间完成较旧的局部会话历史，不增加扫描限制或不重建兼容缓存
- OpenCode Go：以正确比例显示 API 百分比，1% 用量不再显示为 100%，包括本地历史叠加层
- Antigravity：在菜单栏值和每周值中单独显示最受限的已知配额，避免未使用的模型系列隐藏已消耗的配额
- Claude：尊重 Extra Usage 上限的已用/剩余填充偏好，耗尽的上限在剩余模式下为空
- OpenRouter：一致地将 API 密钥支出限制标记为上限，与账户余额分开
- 菜单栏：明确 All providers 是默认布局，显示已保存的提供商覆盖，并提供“Use all-providers layout”选项移除这些覆盖
- 性能优化：Codex 减少加载缓存成本报告所需的工作量，包括项目和会话详情，而不加载原始扫描器状态


### [SagerNet/sing-box v1.13.20](https://github.com/SagerNet/sing-box/releases/tag/v1.13.20) <sup>Qwen/Qwen3.5-27B</sup>

**新内容**
- 修复和改进


### [beck-8/subs-check v1.6.3](https://github.com/beck-8/subs-check/releases/tag/v1.6.3) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增 Claude Code GitHub Workflow
- 升级 mihomo 至 v1.19.30+
- 升级 sub-store 至 2.36.40

**问题修复**
- 优化流媒体检测逻辑
- 跳过 fork PR 的 Claude 审查并添加必需的写入权限



**帮助我们改进**

对今日摘要质量有建议? 欢迎反馈！

- [反馈摘要质量](https://github.com/houzixiashanxiedaima/AstroBlog/issues/new)
- [评价今日日报](https://github.com/houzixiashanxiedaima/AstroBlog/issues/new)

您的反馈将帮助我们优化 AI Prompt,提升摘要准确性
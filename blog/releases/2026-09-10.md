---
title: GitHub Releases 日报 - 2026-09-10
description: 2026-09-10 GitHub Releases 日报：srwi/EverythingToolbar、mack-a/v2ray-agent、zed-industries/zed 等18个项目更新 | 新增搜索窗口可自定义背景样式，包含透明度和亮度滑块 | Xray 支持 VLESS XHTTP TLS 任意门模式
author: Release Bot
pubDatetime: 2026-09-10T08:00:00.000Z
draft: false
tags: ["releases", "daily", "automation", "EverythingToolbar", "v2ray-agent", "zed", "hermes-studio", "tldraw"]
category: 开源追踪
ogImage: /images/covers/cover-2026-09-10.webp
---
### [srwi/EverythingToolbar 3.3.0](https://github.com/srwi/EverythingToolbar/releases/tag/3.3.0) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增搜索窗口可自定义背景样式，包含透明度和亮度滑块
- 支持使用任务栏强调色作为搜索窗口背景
- 改进通过 Crowdin 提供的翻译

**问题修复**
- 修复托盘图标锯齿问题
- 修复桌面工具栏中偶尔丢失按键的问题
- 修复任务栏搜索框位置问题


### [mack-a/v2ray-agent v3.5.24](https://github.com/mack-a/v2ray-agent/releases/tag/v3.5.24) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- Xray 支持 VLESS XHTTP TLS 任意门模式
- 新增协议 14 的部署、账号管理与订阅输出功能
- 配置更新增加校验和失败回滚机制
- 同步中英文脚本及 Cloudflare 连通性检测地址

**Breaking Changes**
- 停止跟踪本地文档和测试目录，相关本地文件不再纳入版本控制


### [zed-industries/zed v1.19.2](https://github.com/zed-industries/zed/releases/tag/v1.19.2) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增调用层次结构支持，可通过快捷键查看传入和传出调用
- Git 面板新增多选功能
- 未命名缓冲区自动检测语言类型
- 默认启用项目搜索时输入即搜功能
- 支持渲染 LSP 3.18 Markdown 诊断消息
- AI：支持使用 OpenRouter 模型时的基于努力的推理
- AI：支持从线程侧边栏重命名终端线程
- AI：新增取消 ChatGPT 订阅设置的按钮
- AI：改进 OpenAI 兼容提供商的流式传输
- AI：优化试用描述，显示 14 天内 $5 GPT Luna 和无限制编辑预测
- Git：文件标签和项目面板新增在远程打开文件和复制远程文件 URL 选项
- Git：改进包含大量大型合并冲突的项目差异打开性能
- Vim/Helix：支持 m i m 和 m a m 的最接近对文本对象
- 语言：CSV、TSV、SSV 和 PSV 文件新增表格数据预览快捷键
- 语言：为 .bash_login 文件添加 shell 脚本语言检测
- 终端：vi 模式下新增 V 键视觉行选择
- 其他：新增 outline_panel.multi_buffer_hide_symbols 设置以在多缓冲区视图中仅显示文件和目录
- 其他：新增 reveal_if_open 设置以在另一窗格中显示已打开的缓冲区
- 其他：Settings UI 新增 close_panel_on_toggle 设置并改进文档
- 其他：新增 folder_indicator 设置以在项目、大纲和 Git 面板中同时显示展开箭头和文件夹图标
- 其他：通过按最近使用排序和改进过滤排名来优化命令面板顺序
- 其他：改进 Zed 退出、更新后重启或重新加载工作区时的数据持久性
- 其他：改进内联代码在所有 Markdown 表面上的圆角背景渲染
- 其他：改进大纲字体和颜色
- 其他：将表格数据预览中的列过滤图标替换为过滤漏斗

**Breaking Changes**
- 默认启用项目搜索时输入即搜功能；设置 { "search": { "search_on_type": false } } 可恢复先前行为

**问题修复**
- 修复 Git Graph 中搜索完整 SHA-256 提交哈希的问题
- 修复 Gemini 拒绝无法表示为受限架构方言的工具架构的请求
- 修复 Helix 模式环绕命令在编辑后未能保留选区和执行缓慢文本边界扫描的问题
- 修复多行注释和多行粘贴后的错误缩进
- 修复 Ollama 未作为编辑预测提供商出现且其模型下拉框仅显示配置模型的问题
- 修复浅层差异导致过度计算的问题
- 修复语言模型提供商流式传输大型工具输入时 UI 挂起的问题
- 修复 .dockerignore 文件在捆绑图标主题中显示通用图标而非 Docker 图标的问题
- 修复 macOS 上 Agent 终端工具调用泄漏文件描述符的问题
- 修复 Anthropic "prompt is too long" HTTP 400 错误未被识别为上下文窗口溢出的问题
- 修复书签标签页在打开时添加或删除书签后未能更新的问题
- 修复断点列表未在单文件中显示断点的问题
- 修复分数 UI 字体大小下容器因亚像素量可滚动并出现幽灵全高滚动条滑块的问题
- 修复多缓冲区替换文件内容时高亮、内嵌提示或其他位置仍引用旧内容可能导致的崩溃
- 修复范围仅覆盖行终止符（如 Python 中缺少 :）的诊断未加下划线的问题
- 修复远程服务器提供的设置架构中缺失扩展提供的语言服务器的问题
- 修复 Zed 未能识别部分克隆中的 Git 远程仓库的问题
- 修复打开提交差异后 "Go Back" 未返回 Git Graph 的问题
- 修复同步执行器阻塞导致的 GPUI Web 崩溃
- 修复 Windows 上 Which Key 中不一致的快捷键标签
- 修复连接行移除块注释外代码（如指针解引用）前导 * 的问题
- 修复某些语言服务器查找所有引用时的崩溃
- 修复裸 Git 仓库的链接工作树名称和创建路径问题
- 修复标准错误不可用时报告 Wayland 连接错误的 Linux 崩溃
- 修复项目搜索保留已关闭未命名缓冲区结果的问题
- 修复设置包含无弃用项（包括仅配置 url 的上下文服务器）时出现设置迁移提示的问题
- 修复过期的 Git 提交模板被恢复为待处理提交信息草稿的问题
- 修复 Windows 上转换格式错误的 ${ shell 变量引用时的崩溃
- 修复后台线程恐慌后 Windows 进程保持存活的问题


### [EKKOLearnAI/hermes-studio v1.0.3 - v0.7.19](https://github.com/EKKOLearnAI/hermes-studio/releases) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 品牌升级为 Ekko Studio，统一应用图标、官网域名、CLI 命令与产品文档
- 新增原生 OpenCode Free 提供商，无需 API Key 即可建聊和供 Coding Agent 使用
- Ekko 新增可持久化的任务计划与实时进度卡片；后台委派任务未结束时侧栏继续显示工作状态
- 聊天文件链接可直接定位并高亮引用行；工作区文件树支持无留白折叠，保留文件与目录状态
- 桌面端可选择在内置或系统浏览器打开网页链接，内置浏览器面板立即展开，无需等待页面加载完成
- 朗读跳过隐藏思考内容并保留负号、百分号等语义符号；豆包 TTS 支持 0.5–2 倍语速并同步当前配置
- 配合新版 App，新增聊天审批、澄清、任务结束、群聊 Agent 回复和工作流完成或失败的前台通知
- 单聊中经用户确认可只读获取 iOS 步数、睡眠、心率等健康数据（需兼容版本 App，暂不支持 Android）
- 新增 Studio 公告弹窗，仅展示最新公告并记住关闭状态，获取失败不影响使用
- 加入 Agent 自动更新策略基础代码（尚未接入界面或后台更新，不会自动升级 Agent）
- 发布 HStudio Android v1.0.3 版本

**问题修复**
- 修复桌面端反复重启、更新中断与启动失败；升级后保留登录及浏览器数据，并迁移 Windows 开机启动设置
- 修复 Ekko 与 Studio 代理的 Coding Agent 请求 OpenCode 时的会话兼容性
- 浏览器标注支持逐项删除和撤销，并修复关闭标签页或清理标注时的异常
- 恢复 Hermes 与 Ekko 技能列表默认显示的启用和禁用开关
- 配合新版 App，会话分类支持独立分页加载，并正确返回置顶筛选结果与分类总数
- 修复 Codex 多行配置和 Hook 表的保留问题、重启配置过早超时，以及 Docker 可选补丁失败阻塞启动的问题
- 若干琐碎修复（断连后的中继清理、MCP 名称迁移、Windows 图标圆角调整、本地化变更日志等）


### [tldraw/tldraw v5.4.2](https://github.com/tldraw/tldraw/releases/tag/v5.4.2) <sup>Qwen/Qwen3.8-27B</sup>

**问题修复**
- 修复 @tldraw/utils 和 @tldraw/validate 的包元数据，明确声明 MIT 许可证


### [nexu-io/open-design open-design-v0.22.2](https://github.com/nexu-io/open-design/releases/tag/open-design-v0.22.2) <sup>Qwen/Qwen3.8-27B</sup>

**问题修复**
- 修复更新后应用返回不可靠的问题，确保新实例等待旧实例完全关闭后再启动
- 优化更新重启期间的反馈机制，避免后台清理耗时超过十秒时误报退出失败


### [KartikLabhshetwar/better-shot v0.5.0 - v0.5.2](https://github.com/KartikLabhshetwar/better-shot/releases) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增覆盖层设置与可视化布局编辑器，支持标准、共享和极简预设
- 新增从捕获面板直接进行云分享功能，包含进度显示与链接复制
- 新增通用启动与可见性控制，包括登录时启动及 Dock/菜单栏显示选项
- 新增 75 个可自定义快捷键操作，涵盖截图、OCR、录制等场景
- 新增更多捕获与录制快捷键，如窗口捕获、定时区域捕获等
- 新增鼠标释放即捕获功能，匹配经典绘制并释放手势
- 新增 URL scheme 自动化支持，可从 Raycast、Shortcuts 等触发操作
- 新增自动保存录制功能，完成录制后自动保存至指定文件夹
- 改进 DMG 安装程序，采用 Retina 适配背景与清晰的拖拽说明
- 优化三步式引导流程：欢迎 → 权限 → 首次捕获
- 所有权限集中展示，无需下拉菜单，每行解释用途并提供操作按钮
- 新增媒体库功能，可从菜单栏或通用设置中直接打开
- 媒体库采用可调整大小的原生窗口，包含左侧边栏、更大的缩略图、截图/视频及本地/云端筛选、搜索以及最新/最旧排序
- 媒体库卡片提供编辑、显示和云链接操作，并支持确认本地删除
- 新增原生引导流程，包括欢迎、权限、快捷键和首次截图指南
- 新增共享柔和渐变背景，提供10种颜色选项
- 新增截图甲板功能，支持最多5张截图的浮动堆栈管理
- 新增保存捕获设置，允许在保存前将截图保留在甲板中
- 统一捕获和录制栏，整合区域、全屏、窗口、OCR、颜色、定时器和录制功能
- 新增可调整录制区域，支持拖拽/缩放选择
- 新增直接编辑工具，支持模糊和像素化效果
- 新增光标样式选项，包括记录、深色、浅色、圆点和原生手型
- 优化本地导出速度，视频导出复用未更改的光栅，图像导出缓存全分辨率PNG
- 新增自定义剪辑速度，支持0.25x-8x播放速率
- 优化导出提示显示位置，移至屏幕顶部独立面板

**Breaking Changes**
- 移除覆盖层中的删除按钮，标准布局将 Pin 置于左上角，云分享置于右下角
- 移除介绍相关操作，测试版明确标识为预览并禁用真实权限请求
- 要求 macOS 15.0 或更高版本

**问题修复**
- 修复覆盖层分享恢复与取消问题，防止自动关闭干扰
- 修复仅首次启动时显示引导的问题，避免重复提示
- 修复权限重试与恢复机制，确保相机和麦克风请求可重试
- 修复两种外观模式下的按钮对比度问题，提升可读性
- 修复编辑器快捷键处理，使用配置的绑定而非硬编码按键
- 修复捕获面板保存失败时的数据保留问题，提供重试指导
- 修复 Deck 复制不再保存到文件夹的问题，仅复制到剪贴板
- 修复 Cmd+S 在图像编辑器中更新已导出文件的功能
- 修复绘制光标与活动工具不匹配的问题
- 修复自定义剪辑速度支持 0.25x-8x 播放速率的问题
- 统一图像导出标签为 Export，与视频编辑器保持一致
- 修复云分享历史记录问题：未修改的截图或导入的视频现在会记录其云链接
- 当本地文件缺失时，云链接仍然可用
- 媒体库合并了捕获历史、编辑后的图像和录制项目，且不重复其源文件


### [openclaw/openclaw v2026.6.35](https://github.com/openclaw/openclaw/releases/tag/v2026.6.35) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 增强提供商与通道边界安全性，限制不可信响应体并拒绝超大输入
- 提升长时运行任务的可靠性，优化取消、重试及进程流故障处理机制
- 强化内置插件韧性，使本地模型、浏览器等插件能从异常负载中恢复
- 优化工具与工作区使用安全，防止畸形或超大输入中断代理运行
- 改进本地集成稳定性，确保语音、会议等集成在失败时不破坏网关

**安全修复**
- 修复响应与内存安全问题，限制各类集成读取以防止恶意负载耗尽进程内存

**问题修复**
- 修复交付与运行时恢复问题，确保预期故障下保留正确的操作结果
- 更新生成的 Plugin SDK API 基线以验证发布包预检


### [chen08209/FlClash v0.8.97](https://github.com/chen08209/FlClash/releases/tag/v0.8.97) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 重构应用 UI 并更新本地化
- 重构应用层与窗口处理，新增代理身份验证功能
- 重构桌面端运行器、打包及原生构建流程
- 重构 Android VPN 服务与生命周期处理
- 重构桌面插件，新增 Helper 服务和 Rust 桥接
- 重构核心 IPC 与进程生命周期管理


### [nicobailon/pi-subagents v0.67.0](https://github.com/nicobailon/pi-subagents/releases/tag/v0.67.0) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增可选的看门狗回退模型，支持主会话、子代理及特定代理在工具使用前的提供商故障时自动切换
- 新增可移植的 Inspect 命令与终端中立集成点，支持 macOS Ghostty 1.3+ 右侧分屏
- 为定期调度任务新增 quiet: true 选项，成功运行不再唤醒父进程，仅失败或暂停时通知
- 新增可选的看门狗问题，在主会话变更完成前标记潜在的任务漂移风险
- 内置 evidence-auditor 工具，用于检查重要研究声明是否得到来源支持
- 异步工作流子任务完成后立即通知父进程，包含工作流、子任务、结果及位置信息
- 委托和预检阶段支持 per-launch intercomBridge 覆盖及 orchestratorTarget 自定义模板
- 默认 Intercom 桥接提示词独立于父会话，保留自定义模板中的 {orchestratorTarget} 占位符
- 启动警告中现在包含已移除的子工具及其活动限制，但不改变启动行为
- 缩短默认的面向子代理的指令，节省上下文令牌预算用于任务本身
- FleetView 代理拥有稳定的身份颜色标识
- 在移除不兼容的签名 Anthropic thinking 块后，保留 forked 子代理请求的思考级别（需 Pi 0.85.0+）
- 并行审查的发现范围限定于请求的目标，diff 审查仅报告由 diff 引起或暴露的问题
- 简化看门狗澄清流程，移除回复跟踪和强制后续审查，改为单个可见问题加原生继续
- 工作流启动、审查及继续的子任务工作显示基于任务的标签

**Breaking Changes**
- 启动契约现为版本 3，启动绑定投影为版本 2，因此此版本中启动契约摘要发生变化；现有保存的运行仍可恢复
- Anthropic fork 用户在签名思考恢复后保留请求的思考级别需要 Pi 0.85.0 或更新版本

**问题修复**
- 修复 runs.all(...) 接受 runs.run(...) 承诺的问题，包括 items.map(...) 形式，并添加一次性警告说明配置对象要求
- 修复 Intercom 感知预检产生与前台/后台执行相同启动摘要的问题，保持解析后的代理定义独立于运行时桥接变更
- 修复预检期间应用项目优化，使其启动摘要与完成的运行匹配
- 修复转向和后续请求仅在子代理消费后才报告为已送达，并在子代理结束时准确报告未消费的请求
- 修复最终关闭期间排队转向或后续工作仍待处理时保持原生子代理存活的问题
- 修复过期的关闭定时器中止恢复的前台或后台工作的问题
- 修复等待记住的分离后代在其父进程结束前停止，而不中止已产生结果的子代理
- 修复暂停的后台运行因仅适用于完成时的检查而失败的问题
- 修复进程树清理仅在分离后代实际停止后才报告为完成
- 修复允许已批准的子协调器回答其自身子代理的监督者问题，同时保留直接父级所有权和工具限制
- 修复允许只读审查员引用诸如“必须修复”等短语而不被误认为实现请求
- 修复在应用工具限制后保留显式的只读请求，同时拒绝无写入工具的实作工作
- 修复在请求的仓库工具不可用时阻止审查和侦察启动，明确空或受限的工具集仍有效
- 修复扩展包装 Pi 内置功能时保持工作流子工具与选定代理对齐，并将自动扩展仓库 worktree 放置在扩展发现之外
- 修复在启动并行工作流子代理前验证 worktree 仓库和清洁度
- 修复已知子代理数量超过 maxSubagentSpawnsPerRun 的工作流在启动任何子代理前被拒绝，动态计数仍在运行时限制
- 修复在工作流行上显示工作流使用情况，而不是显示重叠或误导性的包装器总计
- 修复保持实时工作流计时器推进，正确嵌套加载的子代理，仅折叠完全表示的重复组，并准确冻结完成的持续时间
- 修复在前台子代理中保留父主题，在分离子代理中初始化主题，并刷新命令结果渲染
- 修复解析完整的 Orca 创建输出，使观察者句柄、标签 ID 和标题正确存储
- 修复在推荐模型时保持配置的看门狗模型和思考级别
- 修复识别 OpenRouter 的状态前缀 401 响应作为工具使用前配置的回退资格
- 修复保持内部 OpenCode 辅助请求与普通 Pi 流量在同一提供商会话中
- 修复在扩展检查最终提示词之前运行子提示词过滤器，保留有意的全局上下文和仅限父级的技能排除
- 修复将代理工具声明与主机可用工具相交，使受限的主机在启动子代理前拒绝不可用的工具
- 修复允许 fast 通过后台恢复和后续往返
- 修复在分离运行器中使用检测到的 npm Pi 包根目录，而不是继承的主机路径
- 修复为官方 Pi 0.85.1 Linux 独立版恢复后台 SDK 会话，同时保留对 Pi 0.85.0 的支持
- 修复在不寻常的包布局中正确解析 Pi TUI 别名
- 修复避免在 Pi 0.85 之前的版本上要求更新的 chord 别名，同时保持必需的运行时别名严格
- 修复在 Windows 上使用 git wt 代替 Worktrunk，以避免 Windows Terminal wt.exe 冲突
- 修复防止手动启动的调度在其下一个自然时间再次触发
- 修复允许删除由早期会话拥有的终端调度，当其确切运行已知已完成时


### [steipete/CodexBar v0.58.0](https://github.com/steipete/CodexBar/releases/tag/v0.58.0) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 新增每日支出账本，支持按天查看令牌、请求和花费，并区分未知金额与零用量
- 优化成本图表，悬停显示日期、成本和令牌数且不改变布局
- 增强菜单自定义功能，可选择可见的使用行、会话或每周百分比窗口，并添加重置倒计时或时钟
- 改进账户配额显示，在紧凑账户行中显示重置时间，保持隐私设置

**问题修复**
- 修复 OpenCodex 本地使用统计问题，确保完整历史的花费和每日账本计数可用
- 修复 Codex 本地成本计算问题，排除子代理边界前的继承记录并刷新缓存计数
- 修复令牌计数溢出问题，将 1000K/1000M 提升为 1M/1B 并处理完整有符号整数范围
- 开发工具：测试运行器在 Python 缺少进程包含 API 时提前失败并提供兼容解释器选择说明


### [hugohe3/ppt-master v6.3.2](https://github.com/hugohe3/ppt-master/releases/tag/v6.3.2) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 脚本文本宽度余量按段应用，并限制 CJK 子句断行
- slice_images 为边缘全宽关键噪声命名
- 工作流第 6 步检查点与 P05 早期门控对齐

**安全修复**
- 加固预览净化器：sanitizeSvg() 仅保留 SVG 命名空间中的显式允许列表元素，修复空命名空间及伪装 HTML 突破标签的残留漏洞
- 本地服务器（实时预览与 Confirm UI）增加 DNS 重绑定防护，非回环 Host/Origin 请求返回 403
- web_to_md.py 默认启用 TLS 证书验证，--insecure 可关闭并给出警告
- web_to_md.py 拒绝非公共获取目标，所有请求及最终 URL 必须解析至公共地址


### [firebase/firebase-tools v15.30.0](https://github.com/firebase/firebase-tools/releases/tag/v15.30.0) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 升级 Firebase SQL Connect 本地工具包至 v3.4.19，包含多项修复与性能优化
- 将本地 dataconnect_execute 工具重命名为 dataconnect_execute_in_emulator…
- 在 ONEMCP_SERVERS 中配置了 SQL Connect (Data Connect) OneMCP 代理服务器，并选择了一组远程工具
- 在 OneMcpServer 中配置了 Mcp-Param-Region HTTP 头变通方案以支持 MCP 路由

**问题修复**
- 修复 PostgreSQL 模拟器的 Bug 并提升性能
- 当代码库中的所有函数被删除时，清理托管服务账号
- 修正 Cloud Tasks 模拟器队列 ID 验证错误信息，使其符合 Cloud Tasks 命名规则
- 在复制延迟和并发冲突时重试 IAM 策略更新
- 防止 Cloud Functions 部署重试期间出现死锁和陈旧的源令牌
- 在遇到 HTTP 400 服务账号传播错误时重试 Cloud Functions 创建


### [jdx/mise v2026.9.4](https://github.com/jdx/mise/releases/tag/v2026.9.4) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- bootstrap: Nix 成为 Linux 和 macOS 上的内置包管理器，支持 nix: 前缀声明及常规 Nix profile 应用
- bootstrap: [bootstrap.packages] 条目新增 env 选择器，仅在指定 mise 环境激活时启用对应包
- packslip: 通过 Packslip 安装的工具现可附带 man 手册页，激活时将工具 man 根目录前置到 MANPATH
- task: 新增 task.quiet 设置（及 MISE_TASK_QUIET），用于抑制 mise 自身的任务消息与命令回显头部
- registry: 注册表新增 basecamp 支持
- 性能优化：dotfiles 历史索引重建速度大幅提升，使用 gix 在进程内重构检查点元数据，避免为每个检查点启动 Git 进程

**Breaking Changes**
- deprecated: task output = "quiet" 模式已弃用，请使用 task.quiet 替代，计划于 2027.9.3 移除
- deprecated: [bootstrap].config_roots 和 mise bootstrap config-roots 命令已弃用…

**问题修复**
- install: 修复懒加载工具的依赖目标也是懒加载时首次使用无法正确安装的问题
- backend: 32 位 ARM 解析现在使用 Go 规范的 arm 架构名称…
- bootstrap: 修复 macOS 上 Homebrew bottles 中硬链接 Mach-O 可执行文件签名不正确导致被 SIGKILL 终止的问题
- packslip: mise lock --platform 现在验证签名的发布清单并记录正确的 URL、校验和、大小和签名者，确保跨平台锁定安装可用
- packslip: glibc Linux 上现在遵循 artifact 的 glibc_min 限制，必要时回退到匹配的静态 musl 构建
- packslip: Windows 安装现在使用正确的 .exe 文件名链接 shim，Unix 保持无扩展名
- packslip: 激活的 man 根目录现在限定于 Packslip 支持的工具版本，且调用者的原始 MANPATH 包含在环境缓存标识中
- prune: mise prune 在无法显示确认提示时不再静默成功退出，而是失败并指导用户传递 --yes 或设置 MISE_YES=1
- generate: mise generate tool-stub 现在尊重 stub 顶层 os 选择器…
- dotfiles: HOME 为符号链接时历史路径过滤器现在正确解析，别名、规范路径和波浪号路径指向同一历史条目
- brew: 发布的 crate 现在包含 tap formula 和 cask 元数据 shim…


### [linkwarden/linkwarden v2.16.3](https://github.com/linkwarden/linkwarden/releases/tag/v2.16.3) <sup>Qwen/Qwen3.8-27B</sup>

**新内容**
- 显著提升了大规模场景下链接、搜索、标签、收藏和仪表盘查询的性能



**帮助我们改进**

对今日摘要质量有建议? 欢迎反馈！

- [反馈摘要质量](https://github.com/houzixiashanxiedaima/AstroBlog/issues/new)
- [评价今日日报](https://github.com/houzixiashanxiedaima/AstroBlog/issues/new)

您的反馈将帮助我们优化 AI Prompt,提升摘要准确性